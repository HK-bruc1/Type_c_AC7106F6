# AC710N RDX USB 通话在线录音评估、分阶段实现与验证方案

> 文档状态：v0.13，分阶段实施中；阶段 2A 已失败并安全回退，阶段 2C 有界 PCM Bridge 短时实机验收通过；阶段 3 Encoder/AI_TX 已完成首轮实机编码对账，单 MIC 通话处理算法因资源约束暂时关闭，Far 欠载连续性修复已通过短时实机复测
> 编写日期：2026-08-22
> 目标工程：AC710N / BR56 Type-C 有线耳机
> 参考工程：`t2616cc-firmware/`（AC701N / BR28）
> 前置文档：`docs/3-2.RDX在线录音业务移植评估与实施路线.md`、`docs/3-3.RDX录音基础设施架构优化方案.md`
> 本文范围：USB Audio 通话期间同时采集近端 MIC 与远端 PC Speaker，编码后通过现有 RDX BLE 在线发送给 APP
> 变更约束：评估、决策、实施记录和验收证据统一收束在本文；音频图只通过 JL 工具修改，不手改生成资源

## 1. 结论

**[推荐设计]** USB 通话录音不应启动现有 `ADC -> AGC -> Encoder -> AI_TX` 智能语音 Pipeline。它必须附着到已经运行的 USB 通话链路，在近端 `EQ` 后和远端 `PCSPK PlaySync` 后分别取 PCM，经有界 PCM Bridge 独立归一为 `16 kHz / mono / 16 bit`，按 20 ms 时间轴混成 mono，最后再复用现有 `Encoder -> AI_TX -> RDX BLE` 在线发送路径。当前阶段已因资源约束从近端主链暂时移除 `CVP_V3`；后续恢复通话处理算法时，Near Tap 仍保持位于通话处理和 EQ 之后，不改变 Bridge 边界。

**[已确认事实]** 目标工程已经实测证明：两个独立 ADC Pipeline 共享 PA3 时会重复设置 ADC 格式并破坏 USB MIC/CVP。USB MIC 当前已经持有最高优先级的 `audio_capture_lease`，所以通话录音必须“借用 USB 已有 PCM”，不能再申请一次采集租约，也不能再打开一条 ADC source。

**[已确认事实]** USB 近端和远端目前不在同一条顶层 Pipeline：`pc_mic` 使用 USB Audio Pipeline `0xDC8D`，`pc_spk` 被明确路由到媒体 Pipeline `0xD96F` 以保留 24-bit 播放。因此，仅在 `USB Audio.x6flow` 中增加 Mixer 不能自动拿到远端 PCM。

**[推荐设计]** 首版对 APP 保持已经验证的会议录音数据合同：`16 kHz / mono / 32 kbps / 20 ms / raw Opus`，每帧固定 80 B，仍由 `AI_TX` 进入现有 RDX Session、Destination 和 BLE 队列。首版将近端和远端混为 mono，不同时引入 stereo Opus、协议元数据和 APP 解码变化。

**[已确认事实，阶段性资源决策]** 阶段 3 首轮实机证明，在当前 BR56 固件同时启用单 MIC `CVP/ANS`、Near/Far Bridge、4 帧异步 PCM 队列、标准 Opus Encoder 和 AI_TX 时，CPU 峰值达到约 94%，最低空闲 RAM 仅约 13 KB，并出现 Cfifo 压力及一次 AEC input full；该组合没有足够的量产资源余量。关闭 `TCFG_AUDIO_CVP_SMS_ANS_MODE` 后，CPU 峰值降至约 73%，最低空闲 RAM 提升至约 55 KB，Cfifo、AEC input full 和 PC MIC FIFO `M` 告警均归零，Opus 仍保持 `4670 * 80 B = 373600 B` 精确对账且编码队列无丢帧。因此当前阶段以“先实现稳定的通话录音业务”为优先，暂时移除单 MIC 通话处理算法，接受近端通话降噪、回声控制和主观听感下降；这是一项可回退的阶段性取舍，不是永久取消通话处理能力。

**[推荐设计]** 后续优化应在通话录音链路稳定、APP 闭环和长稳验收通过后单独推进。可评估更轻量的通话处理配置、减少其他常驻内存、复用工作区或降低算法复杂度；只有同时满足资源余量、通话录音不丢帧、USB 主链无告警和完整听感回归，才允许重新启用通话处理算法，不得为了恢复 CVP 再次牺牲录音稳定性。

**[已确认事实]** 阶段 2A 已证明：当前 JL Studio 4.11.2 + BR56 JLStream 不能按 `0xDC8D`、`0xD96F` 两路 Source 标签驱动 `0x9795` 共享 Mixer 的拓扑完成跨顶层 Pipeline 汇聚，该实验会使 Far/PCSPK 格式协商中止并破坏下行主业务。原生跨流路线已冻结为“失败并回退”；该结论不否定同一顶层 Pipeline 内的标准 Mixer，也不再把 `0x9795` 作为当前数据面。

**[推荐设计]** 路线评估和当前推进顺序如下：

1. JL 原生 `mix_recorder` 跨流汇聚已经完成有界 Spike，并因反压 PCSPK 主链判定失败；
2. 阶段 2C 有界 PCM Bridge 已通过短时实机验收，后续以该边界继续接入 Encoder/AI_TX，不再恢复跨 Pipeline Mixer；
3. 暂不优先把 `pc_spk` 从 `0xD96F` 移回 `0xDC8D`。只有原生跨流和 Bridge 都被证据证明不可行，并经过单独评审后，才允许尝试组合图，避免把录音风险扩散到现有 24-bit 播放路径。

**[APP/硬件待验证]** 在 APP 通话场景 Golden Trace、mono 混录解码、USB 双向稳定性、静音策略和 2 小时耐久测试全部通过前，不得在 Ability 中发布 `RDX_ABILITY_CALL_RECORDING`。

## 2. 证据标签与范围

### 2.1 证据标签

本文所有技术判断使用以下标签：

| 标签 | 含义 |
| --- | --- |
| **[已确认事实]** | 可由当前目标代码、生成配置、既有实机记录或参考工程有效代码路径直接证明 |
| **[推荐设计]** | 本文建议冻结为目标实现的设计决策，尚未代表代码已完成 |
| **[Spike 假设]** | SDK 形态显示可行，但需要最小代码/流程图实验才能确认 |
| **[APP/硬件待验证]** | 编译或静态阅读不能得出结论，必须通过目标 APP 或 BR56 实机验证 |

### 2.2 目标

- **[推荐设计]** USB Speaker 和 USB MIC 都稳定运行时，APP 可显式发起“通话录音”。
- **[推荐设计]** 录音同时包含远端 PC 下行声音和近端 MIC 声音；当前阶段近端为 EQ 后原始 MIC，后续资源允许时再恢复通话处理后的 MIC。
- **[推荐设计]** 不影响 PC 侧正常听音、MIC 上行、HID、USB 枚举和已有 RDX BLE 传输；CVP 当前主动关闭，其效果不作为本阶段通过条件。
- **[推荐设计]** 最大化复用 JL 原生音频节点，以及现有 RDX Engine、Session、Destination、Pause/Resume、Mark、帧队列和 BLE 发送能力。
- **[推荐设计]** 第一版保持现有 mono Opus 线上合同，后续可在明确 APP 合同后升级为 `L=near / R=far` stereo。

### 2.3 非目标

- **[推荐设计]** 本阶段不实现本地文件、SD/eMMC/Flash 存储或文件上传。
- **[推荐设计]** 本阶段不复制 BR28 私有 `EffectDev1/2/3`、`st_opus_enc` 或 RDX 业务直连 Sink 节点。
- **[推荐设计]** 本阶段不把 USB Speaker 活跃直接等同于“正在通话”，也不自动开始录音。
- **[推荐设计]** 本阶段不修改 USB 描述符，不要求 Host 固定为 16 kHz，也不牺牲现有 24-bit 播放能力来换取未经验证的快速接入。
- **[推荐设计]** 本阶段不承诺单边 USB stream 消失后继续用静音填充并保持录音；首版采取完整会话终止策略。

## 3. 术语与所有权边界

| 术语 | 统一含义 | 标签 |
| --- | --- | --- |
| 近端（near-end） | 设备本地 PA3 MIC 在现有 USB MIC 主链中经 EQ 后的语音，不是新开的第二路 ADC；当前阶段未启用 CVP，后续恢复算法时改为 CVP/EQ 后取流 | **[推荐设计]** |
| 远端（far-end） | Host 经 USB Speaker Endpoint 下发、即将播放到 DAC 的数字 PCM，不是扬声器经 MIC 回采的声音 | **[推荐设计]** |
| USB 通话 | USB Speaker 和 USB MIC 双向流都稳定运行的候选业务状态；仅凭 USB 流本身不能区分会议软件、媒体播放或测试工具 | **[已确认事实]** |
| USB 通话在线录音 | RDX Wire scene `1`；由 APP 显式请求，同时记录 USB 近端与远端并在线交付；USB 双向流活跃只是可用条件，不等于用户已授权录音 | **[推荐设计]** |
| 在线会议录音 | RDX Wire scene `0`；以设备本地麦克风为唯一内容来源并在线交付，继续使用独立智能语音 ADC Pipeline，并与 USB MIC 互斥 | **[已确认事实]** |
| Tap | 在不改变主播放/采集出口语义的前提下，将节点输出分支给录音链路 | **[推荐设计]** |
| PCM Bridge | 在相互独立的 Near/Far 音频流与当前采集周期之间传递、对齐 PCM 的有界汇聚边界；它不负责协议发送，也不允许无限缓存 | **[推荐设计]** |
| Record Gate | START/PAUSE/RESUME/STOP 控制的录音分支开关，关闭时不影响 USB 主链路 | **[推荐设计]** |
| Capture Lease | 当前用于仲裁 ADC 采集所有权的 `audio_capture_lease` | **[已确认事实]** |
| Session | 一次 APP START 到最终 STOP/强制终止的业务会话；暂停和恢复不会创建新 Session | **[已确认事实]** |
| Capture Cycle | Session 中一段连续取得有效音频的采集周期；同一 Session 可因暂停、恢复或资源变化包含多个周期 | **[推荐设计]** |
| `capture_generation` | 单调标识同一 Session 内不同 Capture Cycle 的采集代次，用于拒绝旧 PCM 和旧编码帧；它不是 Session ID 或重试次数 | **[已确认事实]** |
| Capture Backend | 为指定录音场景提供编码音频的采集后端；Meeting 与 Call 使用不同 Backend，但共享 Session、Destination 和交付语义 | **[推荐设计]** |
| Source Lost | 当前场景必需的音频源关闭、重启、改变格式或持续无法提供有效内容；它不同于静音、短时欠载或 BLE 断连 | **[推荐设计]** |
| Destination | 消费录音帧并完成交付的业务对象；当前产品的必需目的地是在线 APP，不等同于 Capture Backend | **[已确认事实]** |

**[推荐设计]** 所有权规则冻结如下：

| 资源/对象 | 会议录音 | USB 通话录音 |
| --- | --- | --- |
| PA3 ADC / Capture Lease | RDX Engine 获取租约并打开独立 ADC Pipeline | USB MIC 已持有租约；RDX 只附着，禁止再次 acquire/open ADC |
| USB 主链路 | 不运行；若 USB 抢占则会议录音停止 | USB 子系统拥有，RDX 不得改变其格式或生命周期 |
| 录音 Tap/Gate/SRC/Mixer/Encoder | RDX Engine 拥有 | RDX Engine 拥有，但输入源生命周期受 USB 约束 |
| RDX Session/Destination/BLE 队列 | 复用 | 复用 |
| USB stream 重启/关闭 | 会议录音的资源抢占事件 | 通话录音的源失效事件，触发强制终止 |

## 4. 当前实现事实

### 4.1 当前 RDX 会议录音

**[已确认事实]** 当前目标链路为：

```text
PA3 ADC -> AGC -> Encoder(mono Opus) -> AI_TX -> RDX Engine
         -> Destination -> BLE TX queue -> RDX APP
```

**[已确认事实]** `rdx_record_defs.h` 已冻结参数：

| 参数 | 当前值 |
| --- | ---: |
| Sample Rate | 16000 Hz |
| Channels | 1 |
| Bit Rate | 32000 bps |
| Frame Duration | 20 ms |
| Opus Payload | 80 B/帧 |
| Warm-up | 前 10 帧丢弃 |
| Candidate Queue | 16 帧 |

**[已确认事实]** `rdx_record_audio_br56.c` 解析智能语音 Pipeline `0x5475`，从 ADC source 启动，并通过 `NODE_IOC_SET_ENC_FMT`、Encoder 私有格式和 `AI_TX` 回调固定上述格式。

**[已确认事实]** 当前 `rdx_mvp0_protocol.c` 只接受会议场景的简写和 scene `0` 命令，状态 ACK 也把 scene 固定为 `0`。当前 `rdx_integration_config.h` 的已实现 Ability 集合未包含 `RDX_ABILITY_CALL_RECORDING`。

### 4.2 当前 USB 近端路径

**[已确认事实]** 当前选中的 `TypeCEarphone` USB MIC 路径为：

```text
ADC -> EQ -> Sound Splitter
          |-> PlaySync -> Vol_PcMic -> Channel Expander -> BitWidth Convert -> PCMIC
          `-> Switch(RDX_NearTap) -> Sink_Dev0
```

**[已确认事实]** `pc_mic_recoder_open()` 从 `pc_mic` 对应的 Pipeline `0xDC8D` 以 `NODE_UUID_ADC` 启动，设置 `STREAM_SCENE_PC_MIC` 后交给 UAC MIC Endpoint 读取。当前工具配置 `TCFG_AUDIO_CVP_SMS_ANS_MODE=0`，近端 Tap 格式随 USB MIC 主链协商并由 Bridge 归一到 16 kHz mono。

**[已确认事实]** USB Host 可以重新选择 MIC 的采样率、声道和位宽；格式变化时 `uac_stream.c` 会重启 `pc_mic_recoder`。因此，录音分支不能假定 USB MIC 永远是 `16 kHz / mono`。

**[已确认事实]** USB MIC 使用名为 `usb_audio`、优先级 `0xff` 的 Capture Lease Client，并允许抢占优先级较低的会议录音 Client。它是 PA3 ADC 在 USB 场景下的唯一合法所有者。

### 4.3 当前 USB 远端路径

**[已确认事实]** 当前远端路径为：

```text
PCSPK -> Decoder -> PlaySync -> Vol_PcspkMusic
      -> EQ -> BitWidth Convert -> DAC
```

**[已确认事实]** `jlstream_event_handler.c` 中 `pc_spk` 没有返回 PC Audio Pipeline `0xDC8D`，而是明确返回媒体 Pipeline `0xD96F`，代码注释说明目的是让 PC Speaker 使用 24-bit 音频。

**[已确认事实]** `pc_spk_player_open()` 从 `NODE_UUID_PC_SPK` 解析该 Pipeline，并设置 `STREAM_SCENE_PC_SPK`。远端数据不经过 `pc_mic` 的 `0xDC8D` 顶层流。

### 4.4 已知冲突

**[已确认事实]** 既有实机记录显示，USB MIC/CVP 与第二条 RDX ADC Pipeline 并行时，共享 ADC 的采样格式会被后打开的 Pipeline 改写，并出现 CVP frame size 异常。

**[推荐设计]** 通话录音实现必须满足以下静态约束：

- 不调用 `rdx_record_audio_br56_open()` 的 ADC 会议路径；
- 不为通话录音 acquire 新 Capture Lease；
- 不创建第二个 `NODE_UUID_ADC` stream；
- 不修改 USB 主链路协商格式；
- 不以 MIC 声学回采代替远端数字 Tap。

## 5. JL 原生流程复用评估

### 5.1 SDK 已提供的能力

**[已确认事实]** SDK 中存在 `SDK/apps/earphone/audio/mix_record_api.c`。其入口通过名称 `mix_recorder` 取得保留 Pipeline UUID `0x49EC`，再从 `NODE_UUID_ZERO_ACTIVE` 打开 `file_recorder`。

**[已确认事实]** 原生 `mix_recorder` 的典型控制链包含以下概念：

```text
正在运行的播放/采集 Pipeline
    -> 分支或 Switch（由 get_mix_recorder_status 控制）
    -> 录音 Pipeline 0x49EC
    -> MIXER / SRC / Encoder
    -> WriteFile
```

**[已确认事实]** `jlstream_event_handler.c` 在 `TCFG_MIX_RECORD_ENABLE` 打开时，会把名为 `SW_Rec...` 的节点状态函数绑定到 `get_mix_recorder_status()`。`mix_recorder_stop()` 也明确要求先清录音 status，让 Switch 先关闭，再关闭文件，说明 Gate/Switch 是 JL 原生混合录音生命周期的一部分。

**[已确认事实]** 当前产品仍保持 `TCFG_MIX_RECORD_ENABLE=0`、`TCFG_MIXER_NODE_ENABLE=0`，也没有可被解析的 `0x49EC` 录音流程。阶段 1 已通过 JL 工具启用 `Switch`、`Sound Splitter`、`Sink_Dev0/1`，`SRC` 原本已经启用；这些资源目前只承载被动 Near/Far Tap，不等于 Mixer 或混合录音已经启用。

**[已确认事实]** JL 工具的 `Source`/`Sink` 标签不是 `Source_DevN`/`Sink_DevN` 自定义节点。现有未选中的流程组已经用同名标签建立跨音频流程关系，例如 USB Audio 的 `Source(pcspk)` 对接媒体流程的 `Sink(pcspk)`，LE Audio 的 `Source(lea_media)` 对接媒体流程的 `Sink(lea_media)`。这证明工具具备声明跨流程数据关系的机制，但尚未证明两路独立标签可以同时驱动目标 Mixer。

### 5.2 可以复用与不能直接复用的部分

| JL 原生部分 | 判断 | 标签 | 原因 |
| --- | --- | --- | --- |
| `Switch`/录音状态 Gate 模式 | 复用 | **[推荐设计]** | 可让 RDX 开关录音分支而不停止 USB 主链路 |
| `MIXER` 标准节点 | 优先复用 | **[Spike 假设]** | 可避免手写样本加法，但跨顶层 Pipeline 汇聚能力待确认 |
| `SRC` 标准节点 | 复用 | **[推荐设计]** | USB 两侧格式动态变化，编码前必须统一为 16 kHz mono |
| `Encoder` 标准节点 | 复用 | **[已确认事实]** | 当前会议录音 mono Opus 已通过 APP 实测 |
| `AI_TX` 标准节点 | 复用 | **[已确认事实]** | 当前已接入 RDX encoded-frame 回调 |
| Pipeline `0x9795` / `0x49EC` | `0x9795` 用作阶段 2A 工具 Spike；`0x49EC` 保留为正式录音汇聚候选 | **[推荐设计]** | 当前 JL 工具不允许修改自定义流程顶层 UUID，不能为了名称一致手改 `.x6flow` 或生成资源 |
| `file_recorder`/`WriteFile` | 不复用业务层 | **[推荐设计]** | RDX 需求是在线 BLE，不是本地文件；文件系统会引入无关 RAM、Flash/SD 和异常路径 |
| `mix_record_api.c` 原样启用 | 不采用 | **[推荐设计]** | 它硬绑定文件创建、无返回值的 start/stop 接口和存储设备事件，不符合 RDX Engine 合同 |

**[推荐设计]** 正确复用方式不是调用当前 `mix_recorder_start()`，而是复用其 JLStream 组织模式，建立一个 RDX Call Capture Adapter：解析最终冻结的汇聚 Pipeline UUID、注册 `AI_TX`、配置 Opus、控制 Gate，并把 open/close 结果返回给 RDX Engine。阶段 2A 仅解析 `0x9795`，不提前冻结量产 UUID。

### 5.3 必须先做的 JL 原生 Spike

**[Spike 假设]** 在不接 RDX 协议的最小固件中，建立两个受控 PCM 输入，分别模拟近端和远端，验证它们能否从 `0xDC8D` 与 `0xD96F` 的分支同时汇入自定义 Pipeline `0x9795` 的 Mixer。

**[APP/硬件待验证]** Spike 必须回答：

1. `Sound Splitter/Switch` 的支路能否连接到另一顶层 Pipeline 的 Mixer，还是只允许同 Pipeline 内连接；
2. 两个输入的驱动线程、帧长和时间戳不同，Mixer 是否会阻塞快支路或重复旧帧；
3. 某一路暂时断流时，Mixer 是补零、停流还是释放节点；
4. `SRC` 能否在 Host 16/44.1/48 kHz 和 16/24 bit 变化时正确重新协商；
5. Gate 关闭后 USB 主链路是否完全无音频抖动，重新打开是否出现旧 PCM；
6. `Encoder -> AI_TX` 是否仍稳定产生 20 ms、80 B mono Opus 帧。

**[推荐设计]** 第 1～5 项由阶段 2A 的 PCM Spike 验证，第 6 项由阶段 3 的编码闭环验证。若第 1 项失败，应立即停止在原生跨流图上继续堆叠节点，记录并回退失败实验后转入第 13 节的路线 C；不得由阶段 2A 自动进入路线 B。

## 6. BR28 参考工程结论

**[已确认事实]** BR28 参考 RDX 实现对通话场景使用 `MIC_DAC_TO_STERO_OPUS`。其产品语义是同时记录 MIC 与 DAC，下行和上行保持独立声道，并在 stereo 结果中约定 `L=MIC/near`、`R=DAC/far`。

**[已确认事实]** 参考工程通过私有 `EffectDev1/EffectDev2/SinkDev1` 和 `SourceDev1` 汇接 PCM，`EffectDev2` 直接调用私有 `st_opus_enc`，`SinkDev1` 又直接调用 RDX 数据处理。它不是 BR56 标准 Mixer + Encoder 的现成可移植实现。

**[已确认事实]** 参考工程真正用于 Near/Far 对齐的是 `SourceDev1` 内两只独立 cbuf：`output_buff[0]` 缓存 MIC、`output_buff[1]` 缓存 DAC。`FRAME_POINT=320` 时每只 `OUTPUT_BUFF_SIZE = FRAME_POINT * 4 * 2 = 2560 B`，两只合计 5120 B，另外使用 1280 B 拼成 20 ms 的 `LLLL...RRRR...` 双声道帧。当前仓库全部分支和唯一历史版本均为该值，不存在每只 4 KB 的实现；若按整个汇聚工作区口径近似描述为“约 8 KB”，必须与后续私有 stereo Opus 另行申请的 24 KB 工作区区分。

**[已确认事实]** 参考工程在写入这两只 cbuf 前，分别由 `EffectDev3`（MIC）和 `EffectDev1`（DAC）完成 16 kHz 归一化；读取时要求两路各至少 640 B，否则整帧暂不输出。这个“两路先归一、独立有界缓存、按 20 ms 对齐”的模型可参考，但其 cbuf 写不全只打印字符、无 generation、防旧帧、漂移和持续欠载策略，不能原样照搬。

**[推荐设计]** BR28 可复用的是以下业务语义：

- scene `0` 表示会议录音，scene `1` 表示通话录音；
- 通话录音必须同时包含近端与远端；
- 内部保留 `near/far` 身份，便于将来 stereo 使用 `L=near / R=far`；
- START、PAUSE、RESUME、STOP 的业务状态与音频流生命周期关联。

**[推荐设计]** 不复制以下实现：

- `translation_ear_recoder.c`；
- `EffectDev1/2/3` 与 `SourceDev1/SinkDev1` 的定制实现；
- 私有 `st_opus_enc`；
- RDX 业务直接写入音频节点；
- BR28 对场景和声道的隐式回退；
- 与 TWS、本地存储、LED、马达和提示音绑定的副作用。

## 7. 推荐音频架构

### 7.1 目标拓扑

**[推荐设计]** 首选目标拓扑如下：

```text
远端（Host -> device）

PCSPK -> Decoder -> PlaySync -> Tap Far -------------------------------+
                            |                                           |
                            +-> Vol_PcspkMusic -> EQ -> BWC -> DAC      |
                                                                        v
                                                  SRC -> mono 16k -> Gate Far --+
                                                                                 |
                                                                                 v
                                                                      PCM Bridge Mixer
                                                                                 |
近端（device -> Host）                                                           v
                                                                               Gain
ADC -> EQ -> Tap Near --------------------------------------------+          / Limiter
          |                                                       |              |
          +-> PlaySync -> Vol_PcMic -> Expander -> PCMIC          |              v
                                                                  +-> SRC -> mono 16k
                                                                        -> Gate Near
                                                                                 |
                                                                                 v
                                                              Encoder(mono Opus 32 kbps)
                                                                                 |
                                                                               AI_TX
                                                                                 |
                                                    RDX Engine -> Destination -> BLE -> APP
```

**[推荐设计]** 图中的 `Gain/Limiter` 表示混音增益和饱和保护语义，不强制必须增加独立 Limiter 节点。阶段 2C 在 Bridge 中对 Near/Far 各保留约 6 dB headroom 后求和；不再依赖失败的跨 Pipeline JL Mixer。

### 7.2 Tap 位置

| 分支 | 推荐 Tap | 标签 | 选择理由 |
| --- | --- | --- | --- |
| 远端 | `PCSPK Decoder -> PlaySync` 之后、`Vol_PcspkMusic` 之前 | **[推荐设计]** | Tap 接近设备播放时钟域；避免用户调耳机音量后录音忽大忽小；不记录 DAC 后模拟失真 |
| 近端 | 当前为 `EQ` 之后、USB MIC `PlaySync/Vol_PcMic` 之前；后续恢复算法时为 `CVP_V3 -> EQ` 之后 | **[推荐设计]** | 避免二次 ADC，在 USB 输出同步和声道扩展前保留 mono；当前优先节省 CVP 资源，后续可在不改变 Tap/Bridge 边界的前提下恢复处理后语音 |

**[APP/硬件待验证]** 远端 Tap 位于 PlaySync 后是否能与当前近端 EQ 输出保持可接受的相对时延，需要双音轨脉冲/回环测试确认。后续恢复 CVP 后还需重新测量算法时延；若同步偏差超过产品门限，应优先调整 Tap 或分支延时，不在 BLE 接收端猜测补偿。

**[推荐设计]** 禁止在 DAC 后通过 MIC 声学回采远端。该做法会重复记录远端声音，引入房间响应、啸叫、音量依赖和 CVP 残余回声。

### 7.3 首版声道与混音

**[推荐设计]** 第一版两个分支都转换为 `16 kHz / mono / 16 bit` 后再 Mixer：

```text
record_mono = saturate((near * gain_near) + (far * gain_far))
```

**[推荐设计]** 初始建议 `gain_near = -6 dB`、`gain_far = -6 dB`，为两路同相满幅叠加预留约 6 dB headroom；最终增益由双人对话听音、RMS/peak 和削顶统计调整，不能只凭公式量产。

**[推荐设计]** 内部接口和日志仍使用 `near`、`far` 两个身份，不把它们提前合并成无来源的 `pcm0/pcm1`。后续 stereo profile 才能稳定定义 `L=near / R=far`。

**[APP/硬件待验证]** 当前无 CVP 的近端 EQ 输出与远端数字 PCM 的响度、回声和双讲可懂度必须在 Teams/Zoom/腾讯会议等真实软件下验收；已知主观通话效果可能下降，但不能出现功能性断音、复位或录音数据错误。

## 8. 时钟、SRC、缓冲与溢出

### 8.1 时钟基准

**[已确认事实]** USB MIC 和 Speaker 的 Host 格式及启停可以独立变化，且当前近端和远端位于不同顶层 Pipeline。两路 PCM 的调度周期和缓存水位不能假定完全一致。

**[推荐设计]** 录音输出格式仍以 16 kHz Encoder 的 20 ms 帧为唯一帧合同，每帧固定 320 个 mono sample；但 Bridge 不使用普通软件定时器生成音频时间轴。Near/Far 分别归一化后进入短有界缓冲，以 Near 归一化样本累计为主时钟：预装完成后，每新增 320 个 Near sample 驱动一次 Bridge 消费和混音；Far 通过独立缓冲与单点漂移校正跟随 Near。

**[推荐设计]** 两路都应在接近各自 USB 同步节点之后 Tap。阶段 2C 首轮用流式降采样和低频单点丢弃/复制把漂移限制在固定水位窗内，不丢整帧；阶段 3 听音或格式矩阵若证明质量不够，再在保持 Bridge 边界不变的前提下替换为 JL 标准 SRC，不恢复跨 Pipeline Mixer。

### 8.2 缓冲边界

**[推荐设计]** 每路 PCM Bridge 或汇聚端必须使用固定容量环形缓冲，不允许在音频回调中动态分配。初始 Spike 可按每路 40～80 ms 设计，量产值依据实测调度抖动收敛。

**[已确认事实，2026-08-22 阶段 2C 缓冲决策]** 当前 Spike 每路固定 80 ms，即 `16000 * 0.08 * 2 B = 2560 B`；Near/Far 两只环形缓冲合计 5120 B，与参考工程两只 cbuf 的实际容量完全一致。当前 ELF 中完整 `usb_call_audio_bridge` 静态实例为 `0x20A8 = 8360 B`，其余约 3.2 KB 用于两路归一化暂存、Near/Far/Mix 三只 20 ms 工作帧和统计状态，不应误算成额外无界缓存。

**[推荐设计]** 必须分别统计：

- `near_pcm_in_samples`、`far_pcm_in_samples`；
- `near_src_out_samples`、`far_src_out_samples`；
- `near_underflow`、`far_underflow`；
- `near_overflow_drop_samples`、`far_overflow_drop_samples`；
- `mixer_out_frames`、`encoder_out_frames`；
- 当前/峰值缓冲水位；
- 两路估算 ppm 和累计相位差。

### 8.3 欠载与溢出策略

**[推荐设计]** Near 固定拥有录音输出时间轴；Far 短时低水位允许每帧有界复制少量末样本，仍不足时补零并增加计数，不得仅因 Far 连续欠载重建整条录音时间线。真正的 source lost 由 Tap stop、格式代次变化或超过 100 ms 没有输入回调判定，最终门限由实机长稳冻结。

**[推荐设计]** 溢出时丢弃最旧 PCM，使实时录音尽快回到当前时刻，并记录丢弃样本数；不允许覆盖未消费内存或让 USB 主链路反压卡顿。

**[推荐设计]** USB 主链路优先级高于录音。录音 Mixer、Encoder 或 BLE 拥塞只能丢弃/终止录音，不能阻塞 `PCSPK -> DAC` 或当前 `ADC/EQ -> PCMIC` 主链；后续恢复 CVP 时同样不得阻塞 `ADC/CVP -> PCMIC`。

**[APP/硬件待验证]** 上述 100 ms、40～80 ms 都是首轮 Spike 参数，不是量产结论。

## 9. RDX Engine 与资源模型调整

### 9.1 场景化 Audio Backend

**[推荐设计]** 将 Engine 的音频入口从“固定打开会议 ADC”演进为场景化 Backend：

```text
RDX_RECORD_SCENE_MEETING
    -> meeting backend
    -> acquire Capture Lease
    -> open ADC/AGC/Encoder/AI_TX

RDX_RECORD_SCENE_USB_CALL
    -> usb-call backend
    -> validate PCSPK + PCMIC stable
    -> attach Near/Far taps
    -> open Gate/SRC/Mixer/Encoder/AI_TX
    -> never acquire/open ADC
```

**[推荐设计]** Backend 对 Engine 至少提供 `probe/open/pause/resume/close/get_format`，并使用现有 frame callback 上交编码帧。Engine 继续拥有 Session、Controller、Destination、状态迁移和异常终止。

### 9.2 START 条件

**[推荐设计]** 通话录音 START 必须同时满足：

- APP 显式请求 scene `1`；
- RDX 已完成 AppKey/Bound/加密/CCC/Payload 门控；
- USB Speaker 和 USB MIC 都处于稳定 open 状态；
- 两路实际格式已知，且 SRC/Mixer/Encoder 可完成协商；
- 没有另一个录音 Session 或迁移态；
- RDX Destination 已 attach，固定队列已就绪。

**[推荐设计]** “USB 双向活跃”只用于 START 前置校验，不用于推断录音 scene，也不自动触发录音。

**[APP/硬件待验证]** 稳定 open 的定义建议为两路均收到有效 PCM 且格式在至少 2～3 个调度周期内未变化；具体周期必须由 USB 日志和实机抖动冻结。

### 9.3 运行期事件

| 事件 | 首版处理 | 标签 |
| --- | --- | --- |
| APP STOP | 关 Gate，阻止新帧，排空/丢弃尾帧，关闭录音 Backend，USB 继续运行 | **[推荐设计]** |
| APP PAUSE | 只关闭录音 Gate/Encoder 输入，保留 USB 主链路与 Session | **[推荐设计]** |
| APP RESUME | 清空旧 PCM，递增 `capture_generation`，重新同步两路并开 Gate | **[推荐设计]** |
| Speaker 或 MIC close | 强制终止通话录音，不继续单边录音 | **[推荐设计]** |
| USB 格式重协商/recorder restart | 强制终止当前通话录音；新格式稳定后等待 APP 新 START | **[推荐设计]** |
| USB 拔出/模式退出 | 强制终止，清理 Tap/Gate/队列，不发送旧帧 | **[推荐设计]** |
| BLE 断连/CCC off | 沿用当前在线录音强制停止语义 | **[已确认事实]** |
| PCM 连续欠载/Mixer fault/Encoder fault | 强制终止并上报失败原因 | **[推荐设计]** |

**[推荐设计]** Pause 不释放 USB 的 Capture Lease，也不关闭 `pc_mic_recoder` 或 `pc_spk_player`。Resume 必须创建新的 `capture_generation`，防止暂停前的 PCM 或 Opus 候选帧进入恢复后的时间轴。

### 9.4 生命周期顺序

**[推荐设计]** START 顺序：

```text
validate scene/link/USB formats
    -> attach Destination
    -> create/configure call Backend with Gate closed
    -> clear PCM and encoded queues
    -> wait near/far ready
    -> start Encoder/AI_TX
    -> open Gates
    -> receive first valid encoded frame
    -> send START success ACK
    -> allow stream packets
```

**[推荐设计]** STOP/强制终止顺序：

```text
close frame gate
    -> close near/far record Gates
    -> stop Encoder/AI_TX/Mixer/SRC
    -> cancel queued old-generation frames
    -> detach Destination
    -> emit final state/cause
    -> leave USB main streams untouched
```

## 10. Wire 合同与 Ability 演进

### 10.1 场景命令

**[已确认事实]** 参考工程的协议定义为 scene `0=会议`、scene `1=通话`；目标工程当前 parser 只接受会议 scene `0`，且状态 ACK 中 scene 固定为 `0`。

**[推荐设计]** 通话录音必须只由明确的 scene `1` 命令进入。按当前命令形态，候选 START 为 `*APP#record#1#1#1#`，但该字符串在目标 APP Golden Trace 确认前不得冻结为量产合同。

**[APP/硬件待验证]** 必须从目标 APP 抓取并确认以下内容：

- 通话 START/STOP/PAUSE/RESUME 的完整字节串；
- scene、sample-rate-format、record-mode 字段含义；
- 通话 Ability 打开后 APP 页面和命令行为；
- APP 是否接受 scene `1` + mono Opus；
- APP 是否根据 scene 强制按 stereo 拆声道；
- START 失败和运行期 USB source lost 的期望状态 ACK。

### 10.2 第一版线上格式

**[推荐设计]** 第一版仍使用：

```text
16 kHz / mono / 32 kbps / 20 ms / raw Opus
payload = 80 B
packet  = *DEV#stream#80#80# + 80 B payload
```

**[已确认事实]** 该 mono 合同已在会议录音中由同一 APP 实测清晰，BLE 队列、98 B Notify、DLE 和 2M PHY 也已有验证基础。

**[APP/硬件待验证]** 会议场景接受 mono 不等于通话 scene `1` 一定接受 mono。若 APP 对通话强制 stereo，则必须建立独立的 `CALL_V2_STEREO` profile，而不是悄悄复用 `MEETING_V1` 的 format id。

### 10.3 stereo 后续演进

**[推荐设计]** stereo 只作为第二阶段候选：`L=near / R=far`，16 kHz、20 ms。开始实现前必须先确认 APP TOC/声道语义、目标码率和最大帧长。

**[已确认事实]** Opus stereo 即使仍配置为 32 kbps、平均帧可能仍接近 80 B，TOC stereo 位、解码声道数和内容语义已经改变，因此不能以“包长没变”判断兼容。

**[推荐设计]** stereo profile 需要新的 format id、独立最大帧边界、APP Golden Trace、离线双声道解码和左右声道串扰验收。

### 10.4 Ability 发布门槛

**[已确认事实]** `RDX_ABILITY_CALL_RECORDING` 常量已经存在，但目标 `RDX_CFG_IMPLEMENTED_ABILITY` 尚未包含它。

**[推荐设计]** Ability 发布顺序：

1. 先在内部构建完成无 Ability 的音频 Spike；
2. 完成目标 APP scene `1` Golden Trace；
3. 完成 mono call profile 和异常路径验收；
4. 增加编译期 `RDX_CFG_CALL_RECORDING_ENABLE` 及依赖检查；
5. 最后将 `RDX_ABILITY_CALL_RECORDING` 加入已实现 Ability。

**[推荐设计]** 不允许先广播 Ability 再让 APP 试错，也不允许通话能力失败时静默降级为会议录音。

**[推荐设计]** 内部 PCM Spike 和受控联调使用不改变外部广播合同的调试门控。若开发工作区提前把 `RDX_ABILITY_CALL_RECORDING` 置入产品 Ability，而 `RDX_CFG_IMPLEMENTED_ABILITY` 和真实业务仍未实现，该状态只是不一致的开发中间态，必须在进入 Spike 构建前撤销或改为内部门控。

## 11. 隐私、静音与音量策略

### 11.1 MIC 静音

**[已确认事实]** 推荐近端 Tap 位于 `Vol_PcMic` 之前。当前该位置取得 EQ 后 mono，后续恢复算法时取得 CVP/EQ 后 mono；两种情况下都可能绕过 UAC MIC mute/volume 的最终效果。

**[APP/硬件待验证]** 产品必须明确选择以下语义之一，不能由实现偶然决定：

| 策略 | 录音行为 | 影响 |
| --- | --- | --- |
| A：服从 UAC MIC mute | Host 将 MIC mute 后，近端录音也静音 | 隐私直观，但录音内容受 Host 控制 |
| B：录音独立于 UAC MIC mute | Host 听不到近端时，RDX 仍可录近端 | 录音连续，但必须有清晰授权/提示，隐私风险高 |

**[推荐设计]** 默认推荐策略 A。若产品明确选择 B，必须补充用户可见的录音状态、法规/隐私评审和硬件按键静音语义测试。

### 11.2 Speaker 静音与音量

**[推荐设计]** 远端 Tap 位于播放音量之前，所以用户把耳机听音音量调低时，录音仍保持远端原始数字响度；这符合“记录通话内容”而不是“记录耳朵实际声压”的目标。

**[APP/硬件待验证]** UAC Speaker mute 时是否仍记录远端需要产品确认。默认建议 Speaker mute 只影响 DAC，不影响远端录音；若法律或产品定义要求静音即不录，则 Gate 必须显式订阅 mute 状态。

### 11.3 授权与可见状态

**[推荐设计]** 通话录音仍必须经过现有 RDX 绑定、加密和 CCC 门控。USB 枚举或通话开始本身不构成录音授权。

**[推荐设计]** 录音的开始、暂停、恢复和停止应保持现有 APP 状态可见性。任何 USB source lost 导致的强制停止都必须让 APP 最终回到 stopped，而不是只在设备本地静默关闭。

## 12. 可观测性与诊断

**[推荐设计]** 每次 START 打印一条不含音频内容的结构化摘要：

```text
[RDX][CALL] start request=<id> session=<id> capture=<id>
            near=<sr/ch/bit> far=<sr/ch/bit>
            out=16000/1/16 opus=32000/20ms scene=1
```

**[推荐设计]** 运行期每 5～10 s 限频打印：

- near/far 输入样本数和当前格式；
- 两路 cbuf 当前/峰值水位；
- SRC 输出和估算漂移；
- Mixer 输出帧、peak、clipping count；
- Opus produced/enqueued/sent/drop；
- BLE 队列水位和 send fail；
- 当前 USB Speaker/MIC open、mute、format generation；
- heap、物理内存、最大连续块和 CPU 峰值。

**[推荐设计]** 每次 STOP 必须打印可对账摘要和唯一 cause，例如：`user_stop`、`usb_spk_lost`、`usb_mic_lost`、`usb_format_restart`、`pcm_underflow`、`pcm_overflow`、`encoder_fault`、`link_lost`、`ccc_off`。

**[推荐设计]** 调试版提供短时 PCM 抓取点：Near Tap 后、Far Tap 后、Mixer 后。抓取只能由编译开关启用，使用固定时长，并在量产构建中关闭。

## 13. 备选路线与决策矩阵

| 路线 | 说明 | 对现有 USB 影响 | 复用 JL 程度 | 主要风险 | 结论 |
| --- | --- | --- | --- | --- | --- |
| A：JL 原生跨流 `mix_recorder` 模式 | 在 `0xDC8D`、`0xD96F` 增加 Tap/Gate；阶段 2A 汇入 `0x9795` Mixer，量产汇聚 UUID 后续冻结 | 低到中 | 最高 | 跨顶层 Pipeline 汇聚能力未确认 | **[推荐设计] 首选，先 Spike** |
| B：把 PCSPK 移回 `0xDC8D` 组合图 | 近端、远端和录音分支置于同一 USB Audio Pipeline | 高 | 高 | 可能回退 24-bit 播放；流程协商和共存策略变化大 | **[推荐设计] 保留；A/C 均失败后单独评审** |
| C：有界 `SinkDev/SourceDev` PCM Bridge | 两条现有流把 PCM 写入录音汇聚层，由最终冻结的汇聚 Pipeline 读取 | 中 | 中 | 需要处理锁、生命周期、时钟漂移和溢出 | **[推荐设计] A 失败后的首选 fallback** |
| D：再次启动智能语音 ADC | 保持会议流程并用 MIC 回采远端 | 高 | 低 | 已知破坏 CVP；远端质量差且重复回声 | **[已确认事实] 禁止** |
| E：直接移植 BR28 私有节点 | 复制 `EffectDev/SourceDev/SinkDev/st_opus_enc` | 高 | 低 | 芯片/SDK差异、业务耦合、维护成本高 | **[推荐设计] 不采用** |
| F：只录 PCMIC Endpoint 输出 | 只记录近端上行 | 低 | 高 | 完全缺少远端，不满足需求 | **[推荐设计] 不采用** |

**[推荐设计]** 路线 A 的完整退出门槛是阶段 2A 通过第 5.3 节第 1～5 项，并在阶段 3 通过第 6 项。若只证明 Mixer 能混音但会阻塞 USB 主链路，也应判为失败。

**[推荐设计]** 路线 A 失败后必须保留流程截图、串口日志、Near/Far/Mix 计数和唯一失败原因，回退跨流 Spike，再进入路线 C；不允许同时保留两套半成品数据面。

**[推荐设计]** 路线 C 必须证明 Near/Far push 不阻塞 USB、缓冲水位可对账、20 ms 输出不被某一路回调绑死、长期漂移有界、旧 generation 不会泄漏。若路线 C 也失败，停止功能扩展并形成专项分析，不自动进入路线 B。

**[APP/硬件待验证]** 路线 B 启用前必须单独评审，并回归 24-bit PC Speaker、16/24 bit 切换、44.1/48 kHz、DAC 音质和 CPU/RAM，不能把“能出声”视为等价替代。

## 14. 分阶段实施计划与证据台账

所有阶段都必须遵守第 4～12 节的架构和数据合同。每个阶段只解决一个不确定性，达到退出条件后才进入下一阶段；编译通过不能替代 PCM、USB、APP 或硬件证据。

### 阶段 0：冻结 APP 合同与 USB 基线

**[推荐设计]** 工作项：

1. 使用目标 APP 和支持通话录音的参考固件抓取 scene `1` START/STOP/PAUSE/RESUME Golden Trace；
2. 记录 Ability、状态 ACK、record mode、format、stream header 和异常后的最终状态；
3. 确认 APP 是否接受 scene `1` + mono raw Opus；
4. 冻结 source lost、重复命令、BLE 断连和 CCC off 的 APP 期望；
5. 记录当前 USB Speaker/MIC 格式矩阵、RAM、CPU 和 10 分钟双向音频基线；
6. 冻结 Near/Far PCM 抓取方法和测试素材；
7. 清理“外部 Call Recording Ability 已置位、实际实现仍关闭”的中间态，阶段 1～5 只使用内部 Spike 门控。

**[APP/硬件待验证]** 退出条件：Wire 合同无猜测字段，mono/stereo 结论明确，当前无 CVP USB 基线无未解释掉音、FIFO 告警或重枚举；后续恢复通话处理算法时再补充 CVP/AEC 异常基线。若 APP 强制 stereo，先更新第 10.3 节 Profile 决策再进入实现。

### 阶段 1：USB 状态提供者与被动 Tap

**[推荐设计]** 工作项：

1. 提供只读 Speaker/MIC open、格式和 format generation；
2. 使用 JL 工具在第 7.2 节位置加入 Splitter/Tap，录音 Gate 默认关闭；
3. 调试构建分别抓取固定时长 Near-only、Far-only PCM；
4. 记录两路采样率、声道、位宽、帧长、回调任务和调度周期；
5. 验证 mute/volume 对 Tap 数据的实际影响，但不在本阶段替产品冻结隐私策略。

**[已确认事实，2026-08-22 实施记录]** 本阶段已经完成以下代码和静态资源闭环：

1. `uac_stream` 已提供 Speaker/MIC 的 `interface_open`、`stream_active`、采样率、声道、位宽和 `format_generation` 只读快照；Speaker 以真实 PCM 到达作为 active，MIC 以采集租约和 PC MIC recorder 成功打开作为 active；
2. USB Audio `0xDC8D` 的 `TypeCEarphone` 近端图已在 `EQ47` 后、`播放同步/config397398` 前加入 `Sound Splitter -> Switch(RDX_NearTap) -> Sink_Dev0` 旁路；
3. 媒体 `0xD96F` 的 `TypeCEarphone` 远端图已在 `播放同步/9B3B3` 后、`Vol_PcspkMusic` 前加入 `Sound Splitter -> Switch(RDX_FarTap) -> Sink_Dev1` 旁路；
4. `usb_call_audio_tap` 提供默认关闭的 Near/Far Gate、流格式代次、帧/字节/丢弃统计和单消费者接口；消费者运行在音频上下文，只允许向有界缓冲快速复制，Tap 层不依赖 RDX Protocol、不编码、不发送 BLE；
5. `Sink_Dev0/1` 分别只接 Near/Far PCM；节点启动/停止同步更新 Tap 格式和代次，数据回调不申请 ADC、不改变 USB 生命周期；
6. JL 工具生成配置已经启用 `Switch`、`Sound Splitter`、`Sink_Dev0` 和 `Sink_Dev1`，位宽分别覆盖近端 16-bit 与远端 32-bit；
7. 变更节点开关后已执行 `winmk.bat clean` 和 `winmk.bat all`。新增 Tap、两只 Sink 节点及 JL 标准 Splitter/Switch 均实际进入编译/链接，固件完成打包并下载到在线目标板；该结果只证明构建闭环，不等价于 PCM、USB 或音质硬件验证。

**[已确认事实，2026-08-22 实机 PCM 证据]** 阶段 1 调试固件只注册轻量统计消费者，同时打开 Near/Far Gate；不编码、不发送 BLE、不保存 PCM。`COM3_2026-08-22_13-42-17.log` 已确认：

1. Near 实际格式为 `16 kHz / mono / 16-bit / Q15`，稳定区间每秒约 `62～63` 次回调、`31.7～32.3 KB`，与 `16000 * 1 * 2 B` 的理论吞吐一致；
2. Far 实际格式为 `96 kHz / stereo / 32-bit storage / Q23`，稳定区间每秒约 `199～201` 次回调、`764～772 KB`，与 `96000 * 2 * 4 B` 的理论吞吐一致；
3. Far-only 区间 Far `peak_pm` 可达 `461～497`、Near 基本保持低电平；Near-only 区间 Near `peak_pm` 可达 `489～609`、Far 多次为 `0`，并出现两路同时有响应的短区间，证明两只 Sink 没有接反且不是同一份 PCM；
4. 日志内 95 条区间统计全部为 `drop=0`；稳定通话期间 CPU 保持约 `43%～45%`，周期 RAM 当前空闲稳定在 `43～44 KB`，未见持续恶化；
5. 未发现 ASSERT、WDT、异常退出或持续 FIFO 反压。两次 `Cfifo readlock samples is too much` 分别位于首次启流和停流边界，没有在稳定通话期间连续出现；
6. 首次停流后 Near/Far 均由 `generation=1` 更新为 `2` 且变为 inactive；Far 随后以 `generation=3` 重新启动并在关闭时更新为 `4`，证明 Far 的代次和启停检测有效。

**[已确认事实，2026-08-22 双向重拨与静音复核]** `COM3_2026-08-22_13-50-22.log` 补齐了阶段 1 的生命周期和内容行为证据：

1. 第一段完整双向通话使用 Near `generation=3`、Far `generation=9`，挂断后分别更新为 `4` 和 `10`；第二段完整双向通话重新以 Near `generation=5`、Far `generation=15` 启动，结束后分别更新为 `6` 和 `16`，证明 Near/Far 都能在重拨后进入新采集代次；
2. 静音区间内对应 Tap 的 `peak_pm/mean_pm` 可连续归零，但 `active=1`、帧数和字节吞吐保持正常，静音没有被误判成 source lost，也没有停止另一方向；
3. 双方说话区间两路同时持续有响应，例如设备时间 `00:09:26～00:09:30` 内 Near `peak_pm=20～54`、Far `peak_pm=82～94`，另有 `00:09:34` 的 Near `96`、Far `153`，两路没有互斥或串成同源；
4. 全日志 136 条区间统计全部为 `drop=0`，CPU 仍约为播放流 `34%～35%`、采集流 `7%～8%`，通话期间周期 RAM 空闲约 `40～46 KB`，停流后恢复为 `130 KB`；
5. 未发现 ASSERT、WDT、异常退出、fail/error 或持续 FIFO 反压。两次 `Cfifo readlock samples is too much` 都位于完整双向流启动边界；Host 在通话间隙产生的多次 Far-only 短时 open/close 均被独立代次正确识别，没有错误启动 Near；
6. 结合前一份 Gate 关闭基线和测试人员本次双方互通操作，Gate 关闭/打开、内容静音、双讲、挂断和重拨均未发现 USB 主链路回归。

**[已确认事实，阶段 1 结论]** 阶段 1“USB 状态提供者与被动 Tap”通过。该结论只证明 Near/Far PCM 来源、格式、吞吐、静音行为和启停代次可用于后续汇聚，不代表 Mixer、SRC、Opus、RDX BLE 或 APP 通话录音已经实现。临时诊断开关恢复为 `0`，Near/Far Gate 回到默认关闭；关闭诊断后已重新执行 `winmk.bat clean` 和 `winmk.bat all`，编译、链接及固件打包成功，最终 ELF 不含诊断初始化入口或诊断字符串。Host 音量对最终录音响度以及 mute 的产品隐私语义继续按第 11 节在后续混音/APP 阶段收敛，不阻塞阶段 2A。

**[APP/硬件待验证]** 退出条件：Near/Far 来源正确且清晰，Gate 关闭时无分支数据，Gate 开关和抓取不影响 USB 主链路，关键 Host 格式可被准确报告。

**[推荐设计]** 回退点：关闭或删除调试 Tap 即恢复阶段 0 基线，不把未验证分支带入量产资源。

### 阶段 2A：JL 原生跨 Pipeline PCM Spike

**[推荐设计]** 进入条件：阶段 1 的 Near/Far 独立 Tap 均通过。

**[推荐设计]** 工作项：

1. 使用 JL 工具在固定 UUID `0x9795` 的自定义流程中新建并选中 `RDX_Call_PCM_Spike` 子分组，不接 Encoder、AI_TX 和 RDX Protocol；
2. 在 Near/Far 现有受控分支中保留阶段 1 观察点，再分别增加唯一命名的跨流程 `Source` 标签；在 `0x9795` 子分组中通过工具属性分别选择对应 Near/Far Source，由工具保存标签关系，禁止手改 `.x6flow` 补造 `relateId`，也禁止复用已有 `pcspk`、`lea_media` 标签名；
3. `0x9795` 不单独增加启动源，由 `0xDC8D`、`0xD96F` 的受控 Source 标签主动驱动；两路经 Channel/BWC/SRC 统一为 `16 kHz / mono / 16 bit` 后进入 Mixer，Mixer 输出只接 PCM 观察 Sink；
4. 在 Near、Far、Mix 三处增加受控观察点，确认标签关系、实际协商格式和每路输入身份；
5. 验证单边、双讲、短时断流、Gate open/close 和第 15.2 节 Host 格式矩阵；
6. 连续运行至少 10 分钟，记录样本计数、水位、Mixer 输出和 USB 主路统计。

**[已确认事实，2026-08-22 静态拓扑与诊断构建]** 阶段 2A 已进入实机 Spike 前状态：

1. USB Audio `0xDC8D` 与媒体 `0xD96F` 的 `TypeCEarphone` 分组均在原 Tap Switch 后增加第二级 Sound Splitter；阶段 1 的 `Sink_Dev0/1` 观察点继续保留，另一输出分别进入 `Source(rdx_call_near)`、`Source(rdx_call_far)`；
2. 自定义 Pipeline `0x9795` 已启用并选中 `RDX_Call_PCM_Spike` 子分组；Near 为 `Sink -> SRC -> Mixer`，Far 为 `Sink -> BitWidth Convert -> Channel Merge -> SRC -> Mixer`，Mixer 输出只接 `Sink_Dev2`；
3. `Sink_Dev2` 将输出协商约束为 `16 kHz / mono / 16-bit / Q15 PCM`，诊断同时统计 Mix 帧数、字节数、电平、格式、generation、活动实例数和实例冲突；若跨流关系错误地创建两个独立 Mixer/Sink 实例，可由 `instances/conflicts` 直接识别；
4. 阶段 2A 诊断构建临时重新打开 Near/Far Gate 和统计消费者，不接 Encoder、AI_TX、RDX Protocol 或 BLE；该开关只用于本轮 Spike，完成取证后仍须恢复为 `0`；
5. 已执行 `winmk.bat clean` 和 `winmk.bat all`，编译、链接、打包成功；最终 ELF 包含 Mixer、`Sink_Dev2` 和 `[USB_CALL_MIX][DIAG]` 入口，生成的 4055 B `stream.bin` 包含 `rdx_call_near`、`rdx_call_far`、`ChannelMerBF8E4` 和 `MIXER7`。本次构建时下载器为离线状态，因此不把编译结果表述为已烧录或硬件通过。

**[已确认事实，2026-08-22 阶段 2A 实机失败]** 路线 A 已完成两轮有界 Spike，并达到“失败并回退”判据：

1. 第一轮保留阶段 1 的 `Sink_Dev0/1` 观察点。`COM3_2026-08-22_15-01-14.log` 中 PCSPK 每次打开均在格式协商中止；失败节点表包含非法的 `sink_dev1, 0 Hz, 15 ch, 32-bit`，设备侧播放和通话下行无声，但 PCMIC 上行仍可被远端听到；
2. 第二轮删除 `Sink_Dev0/1` 和第二级 Sound Splitter，将两个 Tap Switch 分别直接连接 `Source(rdx_call_near/far)`，重新生成 3947 B `stream.bin`、构建、打包并烧录。`COM3_2026-08-22_15-16-42.log` 已确认新资源生效，失败节点表不再包含 `Sink_Dev0/1`；
3. Near 的 `16 kHz / mono / 16-bit` 分支可协商成功并形成 `Sink_Dev2` 输出；Far 的 `48 kHz / stereo / 32-bit -> BWC -> mono -> SRC 16 kHz` 分支三次打开均在约 100 次协商后 `format negotiation: abort`，`Sink_Dev2` 从未进入 Far 有效运行态；
4. `0xDC8D` 和 `0xD96F` 分别创建流程时都会独立执行 `sink_dev2_bind`，没有形成一个可同时聚合 Near/Far 的共享 Mixer 实例。动态 Mixer 端口编号不是原因：目标工程其他已用 Mixer 同样合法使用 0/2/5/7 等非连续输入端口；
5. 路线 A 已直接破坏 `PCSPK -> DAC` 主业务，违反“录音失败不得反压 USB 主链”的硬门槛。继续调整 Sink 约束或在该跨 Pipeline 图上堆叠 Encoder 没有实施价值，必须先回退跨流 Source/Sink 标签连接。

**[已确认事实，阶段 2A 结论]** JL Studio 4.11.2 与当前 BR56 JLStream 组合下，两个顶层 Pipeline 不能按本 Spike 拓扑汇入一个不影响主链的共享 Mixer。路线 A 状态冻结为“失败并回退”，下一步按既定决策进入阶段 2C 有界 PCM Bridge；该结论不否定同一顶层 Pipeline 内使用标准 Mixer。

**[已确认事实，安全回退构建]** 完成取证后已将 `TCFG_USB_CALL_AUDIO_TAP_DIAG_ENABLE` 恢复为 `0`，并由 JL 工具将 Near/Far 分支分别回退为 `Switch -> Sink_Dev0/1`，删除跨流 Source 标签和第二级 Sound Splitter；重新生成的 3885 B `stream.bin` 中 Source 运行模式为 `UNUSED`。随后执行 `winmk.bat clean` 和 `winmk.bat all`，编译、链接、打包成功，最终 ELF 不含 `[USB_CALL_TAP][DIAG]` 或 `[USB_CALL_MIX][DIAG]` 字符串；下载器在线且已显示 `Download completed`。该结果证明回退固件已烧录，不替代 PCSPK 听感实测。

**[已确认事实，2026-08-22 安全回退实机验收]** `COM3_2026-08-22_15-34-13.log` 与测试人员听感共同闭环：Far 三次均形成 `source -> play_sync -> sink_dev1 -> dac`，Near 形成 `source -> sink_dev0 -> play_sync -> channel_expander -> pc_mic`；全日志 `format negotiation: suss=4`、`abort=0`，无 assert/exception/error 和 `sink_dev2` 运行实例。测试人员已确认本机放歌、通话近端和远端均恢复正常，因此阶段 2A 安全回退通过，不再保留“待 PCSPK 听感确认”状态。

### 阶段 2C：有界 PCM Bridge Spike

**[推荐设计]** 进入条件：路线 A 有明确失败证据，或 JL 工具静态证明无法跨顶层 Pipeline 汇聚。

**[推荐设计]** 工作项：

1. 定义只处理 PCM 的通用 Bridge 和非阻塞 Near/Far push；
2. 每路使用固定容量环形缓冲，初始按 40～80 ms 验证；
3. Bridge 首轮直接接受 Tap 动态格式，在固定计算边界内完成声道合并、Q 值转换和流式降采样；阶段 3 若听音不达标，再只替换归一化器为 JL 标准 SRC；
4. Bridge 校验 `capture_generation` 和 USB format generation，拒绝旧代次输入；
5. 按 20 ms、320 sample 对齐混音，执行第 8 节欠载、溢出和漂移策略；
6. STOP、PAUSE、Resume 和 USB restart 时验证 Gate、缓冲和旧 PCM 清理；
7. Bridge 故障只上报 Backend，不直接改变 Engine 或发送 BLE。

**[已确认事实，2026-08-22 阶段 2C 诊断实现与首轮实机冒烟]** 已完成首版代码、构建和首轮实机闭环：

1. `usb_call_audio_bridge` 作为阶段 1 Tap 的唯一消费者，Near/Far push 不分配内存、不等待、不做 I/O；输入接受 16/32-bit、mono/stereo 和动态采样率，当前实测 Near `16 kHz/mono/Q15` 直通，Far `96 kHz/stereo/Q23` 先合并声道并归一为 16 kHz/Q15；
2. 每路 2560 B 固定环形缓冲，40 ms 预装后按 20 ms/320 sample 输出；溢出丢最旧样本，短欠载补零，连续 100 ms 欠载只终止 Bridge capture cycle，绝不关闭或反压 USB 主链；
3. 水位偏离 40 ms 目标超过半帧时，每周期最多丢弃或复制一个样本作漂移校正；Near/Far 各衰减约 6 dB 后混成 mono，并保留 input/normalized/used、水位、underflow/overflow、drift、stale/invalid、Mix peak/mean 等限频统计；首轮固件按 1 s 输出，发现打印窗口可能扰动 20 ms 定时消费后已改为 5 s；
4. Tap format generation 变化、任一路停止或重启都会清空两路缓冲并递增 Bridge `capture_generation`；输出回调携带 generation，旧代次帧可被后续 Backend 再次拒绝；
5. 阶段 2A 专用 `usb_call_audio_mix_diag` 已从构建清除，`Sink_Dev2` 代码恢复 SDK 原始行为；本阶段仍未连接 Encoder、AI_TX、RDX Protocol、BLE，也未发布 `RDX_ABILITY_CALL_RECORDING`；
6. 首轮 `COM3_2026-08-22_16-00-12.log` 中，Near/Far 分别识别为 `16 kHz/mono/Q15` 和 `96 kHz/stereo/Q23`，Bridge 建立 `timeline=1` 并连续输出约 52.6 s；稳定区间基本保持 `50 frames/16000 samples` 每秒，`underflow/stale/invalid=0`，近端、远端和双讲均有独立电平证据，停止时两路 Tap 关闭、时间线清零且旧代次输出为零；测试人员同时确认近端、远端主通话均正常，首轮非阻塞和主业务安全性通过；
7. 首轮 Far 早于 Near 启动时产生 `26/2033 samples` 的有界启动溢出；排除该启动瞬态后，稳定期 Near/Far 分别溢出 `5/308`、`5/312 samples`，漂移丢样分别为 `1548`、`1428 samples`。两路溢出同步出现且部分统计窗口只有 49 帧，当前只能判断与定时调度/诊断打印相关，不能提前认定为输入速率错误或量产可接受；
8. 已将 `USB_CALL_AUDIO_BRIDGE_REPORT_MS` 从 1000 ms 调整为 5000 ms，重新执行 `winmk.bat all`，编译、链接、打包成功且目标板显示 `Device online`、`Download completed`。当前 `update.ufw` 为 1474816 B，SHA-256 为 `802D545B3D5EEFE5DD96DF569410EADD2AD2A82C7D08DCCD2B6D86B119C701DC`；`stream.bin` 仍为 3885 B，SHA-256 为 `6E59141451EDD02687FE67BE2CE4DF348C2E8F4112B85C8578EADF56388352EC`，说明本轮没有改变可视化音频图资源。

**[已确认事实，2026-08-22 阶段 2C 第二轮场景覆盖与时基结论]** `COM3_2026-08-22_16-20-27.log` 覆盖约 91 s 连续 Bridge 时间线，虽未达到 10 分钟，但已覆盖 Near-only、Far-only、双讲和双方静音：

1. Near/Far 格式仍正确，场景电平与测试操作相符，`underflow/stale/invalid/clip=0`；测试人员确认双方通话正常，CPU 稳定在约 61%～62%，最低空闲 RAM 为 27 KB，无 assert、exception 或格式协商 abort；挂断时 `timeline=0`、`source_lost=1`、`stale_out=0`，因此数据来源、混音内容、主链隔离和生命周期继续通过；
2. 17 个完整 5 s 窗口理论应输出 4250 帧，实际仅输出 4238 帧，缺少 12 帧即 240 ms；Near/Far 溢出均为 14 次，分别丢 962/808 samples，且在相同窗口同步发生。将诊断从 1 s 限频到 5 s 后问题仍存在，证明串口打印不是唯一原因；
3. 当前 `sys_timer_add()` 是同步线程定时器，回调延迟会直接减少混音帧数；同时对 Near 和 Far 都按该较慢时基执行漂移丢样，造成 Near 累计丢 2704 samples、Far 累计丢 2605 samples。该版不能作为最终音频时间轴，阶段 2C 暂不进入 10 分钟验收或 Encoder 集成；
4. 修正版已改为 Near 归一化样本主时钟：Near 水位在 40 ms 预装之上每累计 320 samples 驱动一帧，Near 不再执行漂移丢/复制；只有 Far 根据相对 Near 的水位作单点校正。该轮仍保留的 20 ms `sys_timer` 只监视 Tap 活跃状态、format generation 和持续 100 ms 无输入，不再产生 PCM 帧；
5. 修正版已连续两次执行 `winmk.bat all`，编译、链接、打包成功，ELF 诊断标识为 `clock=near_samples`，目标板显示 `Device online`、`Download completed`。当前 `update.ufw` 为 1475072 B，SHA-256 为 `F6D37313E81941BD46BF9C6881DAF4DD8D49EFEC0DD2C84E747FA906E7A072CD`；`stream.bin` 仍为 3885 B、SHA-256 仍为 `6E59141451EDD02687FE67BE2CE4DF348C2E8F4112B85C8578EADF56388352EC`，可视化音频图未改变；Call Recording Ability 仍未开放。

**[已确认事实，2026-08-22 阶段 2C 事件驱动修正与短时验收]** `COM3_2026-08-22_16-47-07.log` 证明 Near 样本主时钟能够完整闭环，但旧版 20 ms 生命周期 monitor 共触发 21 次 `timer_no_response`；首次完整 Bridge 统计输出后又连续触发超时，随后唯一一次出现 `far_in_full`、`aec_in_full` 和 `aec inbuf full`。测试人员同时确认插入瞬间存在吱声，因此该问题判定为 monitor 在 USB 流切换和诊断打印期间阻塞音频任务，而不是 Near 样本时间线本身失效。最终修正版完成以下收敛：

1. 完全删除 Bridge 的 20 ms `sys_timer` 生命周期轮询；Tap 增加同步 stream observer，在锁外把 Near/Far start、stop、format generation 和 active 状态直接通知 Bridge，Bridge open 时主动读取一次 Tap runtime，避免观察者注册前已有流的状态遗漏；
2. 时间线运行期间若单路超过 100 ms 不再送数，由另一侧 PCM push 触发 source-lost 和两路缓冲清理；首次时间线要求 Near/Far 均达到 `40 ms preload + 20 ms frame = 60 ms`，消费首帧后保留约 40 ms 水位；
3. `COM3_2026-08-22_17-06-21.log` 已确认最终固件标识为 `clock=near_samples lifecycle=tap_event preload_ms=40 start_ms=60`。全日志 `timer_no_response=0`，`far_in_full=0`、`aec_in_full=0`、`aec inbuf full=0`，此前的阻塞因果链不再出现；
4. 17 个完整 5 s 稳定窗口精确输出 `4250 frames / 1360000 samples`。Near 为 `896 + 1359872 - 1360000 = 768`，与最终水位一致，且 underflow/overflow/drift/stale/invalid 全为 0；Far 为 `613 + 1360049 - 1360000 - 104 drift_drop = 558`，同样与最终水位一致，稳定期 underflow/overflow 为 0，漂移校正在前 15 s 后归零；
5. Near-only、Far-only、双讲和双方静音均有独立电平证据，混音 `clip=0`；CPU 稳定约 61%～62%，最低空闲 RAM 为 31 KB。通话停止后的 `source_lost=1` 发生在 Near 已停止、Far 短暂重启的生命周期切换中，不是通话期断流；测试人员确认插入和双向通话听感可接受；
6. 最终 `update.ufw` 为 1475072 B，SHA-256 为 `AA517307419648D162EA32C94EB1FB953A8EFF9A59D4430ACF61443B582741E0`；`stream.bin` 仍为 3885 B，SHA-256 仍为 `6E59141451EDD02687FE67BE2CE4DF348C2E8F4112B85C8578EADF56388352EC`。本阶段仍未连接 Encoder、AI_TX、RDX Protocol 或 BLE，也未发布 `RDX_ABILITY_CALL_RECORDING`。

**[已确认事实，阶段 2C 结论]** 有界 PCM Bridge 的数据来源、格式归一、Near 样本时钟、Far 有界漂移、场景内容、主链隔离和事件驱动生命周期均通过短时实机验收，路线冻结为后续 Encoder/AI_TX 的 PCM 输入基础。测试人员决定不再执行本阶段单独的 10 分钟专项测试；该决定只关闭阶段 2C Spike，不替代 Ability 发布前的 APP 合同、编码解码、异常矩阵和长期稳定性验收。

**[推荐设计]** 失败处理：停止功能扩展并形成专项分析；不得用无界扩容掩盖漂移，也不得自动进入路线 B。

### 阶段 3：标准 Encoder 与 AI_TX 闭环

**[推荐设计]** 进入条件：路线 A 或 C 只能有一条 PCM 汇聚路径通过并被冻结。

**[推荐设计]** 工作项：

1. 建立 USB Call Capture Adapter，封装 `probe/open/pause/resume/close/get_format`；
2. Bridge 输出当前发生在 Near Tap 的 JLStream 音频上下文；阶段 3 必须先通过固定容量输出队列或 SDK 等价异步机制转交编码任务，再接入 BR56 标准 Encoder 和 AI_TX，不得在 Near 回调内直接执行可能等待或反压的编码/发送，也不移植私有 `st_opus_enc`；
3. 固定候选格式 `16 kHz / mono / 32 kbps / 20 ms`，验证每帧 80 B 和预热规则；
4. 主机离线重组 raw Opus，对比 Near-only、Far-only 和双讲；
5. Gate open/close 100 次，检查内存、资源、旧帧和 USB 主路。

**[已实现待硬件验证，2026-08-22]** 阶段 3 首轮 Spike 已按当前 BR56 SDK 的实际接口收敛，不照搬参考工程的私有 `st_opus_enc`：

1. 复用自定义 Pipeline 固定 UUID `0x9795`，JL 工具已新增并选中 `RDX_Call_Encoder_Spike` 子分组，拓扑只有 `Source_Dev1 -> 编码器(A2C10) -> AI_TX` 三个节点和两条连线；它是 Bridge 后的独立编码流，不再建立任何跨顶层 Pipeline Source/Sink 标签，也不连接 USB 主线路；
2. 将 SDK 原占位 `SourceDev1` 实现为可注册的通用异步源。Bridge 回调只把一帧 320 sample mono PCM 复制到 4 帧固定队列并唤醒 `NODE_TYPE_IRQ` SourceDev1；PCM 拉取、Opus 编码和 AI_TX 回调均由 JLStream 工作线程执行，不在 Near Tap 音频上下文内编码或发送；
3. 队列固定为 4 帧/80 ms，满时丢最旧帧以保持实时性；Bridge `capture_generation` 变化时清除旧队列并单独对账 `generation_drop/reset`，不允许旧代次 PCM 进入新时间线；
4. Encoder 候选合同固定为 `16 kHz / mono / 32 kbps / 20 ms / raw Opus`，AI_TX 首轮只统计 `80 B` 帧长和 `pcm in/queued/dequeued/drop/opus`，不连接 RDX Record Engine、BLE 或 APP；
5. 诊断开关与阶段 1 Tap 诊断、阶段 2C Bridge 诊断互斥；工具生成后已关闭纯 Bridge 诊断并打开 Encoder 诊断，执行 `clean` 和 `all` 成功，`source_dev1_file.c` 与编码适配器均实际参与编译、链接和打包；设备在线时构建脚本已自动下载本轮固件；
6. 最终 `update.ufw` 为 1467136 B，SHA-256 为 `453EB6EEE24C8840BA8CB55A2F018D70620D3B4173AB8BB00713615606346D1F`；`stream.bin` 为 3708 B，SHA-256 为 `DC32580A25077EE658AF481F479B276A3DAC77DE8A6AC347A1A292400B2C5E85`。这些证据只证明工具生成和固件构建通过，尚不能视为 Opus 数据或 USB 主业务实机通过。

**[已确认事实，2026-08-22 阶段 3 资源取舍]** 首轮实机已证明标准 Encoder/AI_TX 可以稳定产生固定 80 B raw Opus，但也暴露了当前固件无法同时为单 MIC 通话算法和录音链路保留足够资源余量：

| 指标 | 启用单 MIC CVP/ANS | 关闭单 MIC CVP/ANS |
| --- | ---: | ---: |
| 实测日志 | `COM3_2026-08-22_17-47-27.log` | `COM3_2026-08-22_18-03-59.log` |
| Opus 对账 | `4795 frames / 383600 B`，全部 80 B | `4670 frames / 373600 B`，全部 80 B |
| PCM/编码丢帧 | `overflow_drop=0`、`generation_drop=0` | `overflow_drop=0`、`generation_drop=0` |
| CPU 峰值 | 约 94% | 约 73% |
| 最低空闲 RAM | 约 13 KB | 约 55 KB |
| 资源压力现象 | 28 条 Cfifo 告警、1 组 AEC input full、大量 PC MIC FIFO `M` | 上述三类告警均为 0 |
| 产品影响 | 通话处理效果保留，但系统资源余量不可接受 | 双向通话功能保留，通话降噪/回声处理效果下降 |

**[已确认事实，阶段性决策]** 当前选中的 `TypeCEarphone` 分组已将 `TCFG_AUDIO_CVP_SMS_ANS_MODE` 从 `1` 调整为 `0`，近端主链变为 `ADC -> EQ -> Sound Splitter -> PCMIC/Near Tap`。本阶段验收优先级冻结为：USB 双向通话功能正常、Near/Far 录音内容正确、Encoder/AI_TX 连续、资源有安全余量；通话降噪、回声控制和主观音质暂不作为阻塞项。该选择是为了先完成通话录音业务闭环，不代表产品最终形态不需要通话处理算法。

**[已确认事实，2026-08-22 Far 欠载连续性修复]** 无 CVP 首轮日志中 Bridge `capture_generation` 在通话期仍发生多次内部重建，分析定位为 Far 短时欠载被旧策略误判为丢源，而非 USB 设备复位。Bridge 随后调整为 Near 主时间轴、Far 有界复制/补零，取消“连续 5 帧 Far 欠载即重建”，同时保留 Tap stop、格式代次变化和 100 ms 无输入回调的真实断流检测；Encoder 诊断新增 Near/Far 水位、欠载、溢出、漂移、source-lost 和 stale-output 对账。

**[已确认事实，故障候选已撤回]** 第一版 Far 补样实现遗漏了“Far 水位必须低于一帧”的条件。当水位处于 321～479 samples 时，`320 - ring.count` 在 `u16` 中下溢为约 65K，补样循环随即越过 320-sample 帧缓冲并破坏内存。`卡死.log` 显示 Near Tap 在 `00:00:06.602` 启动后，系统定时诊断全部停止，仅剩 USB 中断侧字符，符合该越界导致音频任务卡死；未出现正常 ASSERT 保护。对应 `update.ufw` 1392896 B、SHA-256 `F1359F16D3841B98191586E7F83E26FB3FEB7429E299B845B5ED869589CB0455` 已撤回，禁止继续烧录或作为阶段证据。

**[已确认事实，修复及短时复测通过]** 补样条件已增加 `ring.count < 320` 硬约束，只允许在 316～319 samples 时复制 1～4 个末样本；复制循环改为以 `read_count < 320` 为唯一写入边界，不能越过工作帧。修复候选执行 `winmk.bat all` 后编译、链接和打包成功；构建时设备离线，脚本未自动下载，本地 `update.ufw` 为 1392640 B、SHA-256 `C28C50E5547484E92E29242ADAB379164FCC976B1B570614D9C1DA1873FAF20B`。

`COM3_2026-08-22_18-28-48.log` 已跨过原卡死点并完成约 82 秒稳定通话：通话期 17 个 5 秒诊断窗口中 `bridge_capture=5`、`encoder_capture=5` 始终不变，除首个启动窗口外均为 `resets=0/source_lost=0/stale_out=0`；Near 欠载、溢出、漂移和无效输入全部为 0，Far 分散补偿 4933 samples、无溢出或无效输入，且未再触发时间线重建。最终 `pcm in/queued/dequeued=4122/4122/4122`，`overflow_drop=0`、`generation_drop=0`、`invalid=0`，Opus 为 `4122 frames / 329760 B = 4122 * 80 B`、`invalid_len=0`。通话期 CPU 为 71%～79%，最低空闲 RAM 为 56 KB，无 Cfifo、AEC input full、timer no response、ASSERT、exception 或运行期复位；挂断后才出现预期的 Tap stop、`source_lost=1` 和代次变化。因此本轮可以确认越界卡死已修复，Far 欠载不再破坏录音连续时间线；长稳、离线 Opus 听音和 APP 闭环仍按阶段 3 后续门槛执行。

**[推荐设计，后续优化]** 通话录音 APP 闭环和长稳完成后，再建立独立的通话效果优化阶段。优先评估轻量算法配置、算法工作区复用、其他常驻模块裁剪和 CPU 峰值削减；恢复任一通话处理算法前必须重新完成 RAM/CPU、Cfifo/AEC、PCM/Opus 对账、双讲听感和长稳回归。若资源余量再次退化到首轮水平，应保持当前无 CVP 基线，不以牺牲通话录音稳定性换取算法开启。

**[APP/硬件待验证]** 退出条件：第 5.3 节第 6 项通过，Opus 可正确离线解码，`produced/enqueued/drop` 完整对账，100 次循环无泄漏、爆音或旧帧，会议录音完整回归。若阶段 2A 选择了路线 A，至此第 5.3 节六项才算全部通过。

### 阶段 4：Record Engine 场景化 Backend

**[推荐设计]** 工作项：

1. 增加 call scene/format/backend，不改变 `MEETING_V1` 含义；
2. 将固定会议 Adapter 演进为 Backend 分发，Engine 不依赖具体 Pipeline UUID；
3. Call Backend START 前校验 Speaker/MIC 稳定且格式已知，且不申请 Capture Lease；
4. Pause 只关闭录音 Gate/编码输入，Resume 清队列并递增 `capture_generation`；
5. source lost、format restart、Bridge/Mixer/Encoder fault 通过可信 system event 强制终止；
6. 复用 Session、Destination、candidate queue、TX Scheduler、Mark 和 BLE 门控。

**[APP/硬件待验证]** 退出条件：内部测试入口可明确选择 Meeting/Call 且二者不能并发；Call start/stop 不改变 USB 主路；强制终止有唯一 cause；会议录音回归通过。

### 阶段 5：scene `1` 协议与目标 APP 联调

**[推荐设计]** 进入条件：阶段 0 Golden Trace 和阶段 4 内部闭环均通过；Call Recording Ability 仍保持关闭，仅使用受控联调构建。

**[推荐设计]** 工作项：

1. Parser 接受已冻结的 scene `1` START/STOP/PAUSE/RESUME；
2. 状态 ACK 使用真实 scene、format 和 record mode；
3. START success 等待 Near/Far、Encoder、AI_TX 和首个有效候选帧就绪；
4. 验证 source lost、BLE 断连、CCC off、重复命令、USB restart 和格式切换；
5. 若 APP 拒绝 mono，停止本阶段并建立独立 stereo Profile，不修改 `MEETING_V1` 冒充兼容。

**[APP/硬件待验证]** 退出条件：APP 行为与 Golden Trace 一致，START/STOP 100 次、Pause/Resume 50 次通过，USB restart 后无旧 Session 自动恢复，会议 scene `0` 回归通过。

### 阶段 6：Ability、配置与量产收敛

**[推荐设计]** 工作项：

1. 增加 `RDX_CFG_CALL_RECORDING_ENABLE` 和完整编译期依赖，保证 source-selection 一致；
2. 完成第 15 节全矩阵、两小时长稳及 RDX/Call ON/OFF clean build；
3. 对比 Map、heap、物理内存、最大连续块、CPU 和功耗；
4. 最后将 `RDX_ABILITY_CALL_RECORDING` 加入已实现 Ability；
5. 使用 JL 工具保存、导出并记录 `.x6flow`、`stream.bin` 和固件版本。

**[APP/硬件待验证]** 退出条件：量产 Ability、APP UI、Wire 命令、真实音频能力和构建配置完全一致。

### 阶段证据台账

每完成一个阶段就在本文更新状态和证据，不以口头“能用”跨过闸门。状态只允许使用：`未开始`、`进行中`、`通过`、`失败并回退`、`阻塞待外部结论`。

| 阶段 | 当前状态 | 路线/结论 | 必需证据 | Commit/日志/固件 |
| --- | --- | --- | --- | --- |
| 0 APP 合同与基线 | 进行中 | Ability 已保持关闭；APP 合同仍待冻结 | clean build 已通过；Golden Trace、USB 硬件基线待补 | 2026-08-22 clean/all、在线板下载成功 |
| 1 被动 Tap | 通过 | Near/Far 来源、格式、吞吐、静音、双讲和双向重拨代次均通过；诊断 Gate 已关闭 | 两次 Gate-on 实机日志共 231 条区间统计 `drop=0`，无稳定期反压或主链回归；关诊断后 clean/all 打包成功 | 2026-08-22 `COM3_2026-08-22_13-42-17.log`、`COM3_2026-08-22_13-50-22.log` |
| 2A 原生跨流 | 失败并回退 | 新旧两版拓扑均使 Far/PCSPK 格式协商 abort；安全回退后主链协商和双方听感恢复 | 两轮失败资源/日志；回退日志 `suss=4`、`abort=0`，放歌和双向通话正常 | 2026-08-22 `COM3_2026-08-22_15-01-14.log`、`COM3_2026-08-22_15-16-42.log`、`COM3_2026-08-22_15-34-13.log` |
| 2C 有界 Bridge | 通过（短时） | 两只 2560 B 环形缓冲与参考工程容量一致；Near 样本主时钟精确闭环；20 ms monitor 已删除并改为 Tap 事件驱动生命周期 | 85 s 稳定区间 `4250 frames/1360000 samples` 精确对账；Near 全部质量计数为 0，Far 漂移有界且归零；`timer_no_response` 和三种 CVP/AEC input full 均为 0；场景内容、插入听感和双向通话通过 | 2026-08-22 `COM3_2026-08-22_16-47-07.log`、`COM3_2026-08-22_17-06-21.log`；最终固件 SHA-256 `AA51...41E0` |
| 3 Encoder/AI_TX | 进行中 | 80 B Opus 和异步队列已通过实机对账；因 CPU/RAM 余量不足暂时关闭单 MIC CVP/ANS；Far 欠载连续性与越界修复已通过约 82 秒短时复测 | 通话期 `capture_generation` 稳定，`4122 * 80 B` 精确对账，CPU 71%～79%，最低空闲 RAM 56 KB；Opus 离线解码、100 次开关仍待完成 | 2026-08-22 `COM3_2026-08-22_17-47-27.log`、`COM3_2026-08-22_18-03-59.log`、`卡死.log`、`COM3_2026-08-22_18-28-48.log`；`F135...0455` 已撤回，修复候选 `C28C...F20B` |
| 4 Engine Backend | 未开始 | Meeting/Call 双 Backend | 状态与故障注入 | 待补 |
| 5 APP 联调 | 未开始 | scene `1` | APP 日志、100/50 次循环 | 待补 |
| 6 Ability/量产 | 未开始 | 发布决策 | 两小时、全矩阵、Map | 待补 |

## 15. 硬件与 APP 验收矩阵

### 15.1 功能矩阵

| 场景 | 检查项 | 通过标准 | 标签 |
| --- | --- | --- | --- |
| Near-only | 本地说话，远端静音 | APP/离线文件只出现清晰近端，无周期噪声 | **[APP/硬件待验证]** |
| Far-only | 播放标准语音，MIC 环境静音 | 远端清晰，电平不随耳机播放音量意外变化 | **[APP/硬件待验证]** |
| 双讲 | 双方同时说话 | 两方均可懂，无一方长期被压制，无明显削顶 | **[APP/硬件待验证]** |
| Host MIC mute | 录音中的 near 行为 | 与第 11.1 节最终产品策略一致 | **[APP/硬件待验证]** |
| Host Speaker mute | 录音中的 far 行为 | 与第 11.2 节最终产品策略一致 | **[APP/硬件待验证]** |
| APP Pause/Resume | USB 通话不中断 | 暂停无新音频；恢复使用新 generation，无旧帧 | **[APP/硬件待验证]** |
| USB MIC close | source lost | 录音强制结束，Speaker 仍按 Host 状态运行 | **[APP/硬件待验证]** |
| USB Speaker close | source lost | 录音强制结束，MIC 仍按 Host 状态运行 | **[APP/硬件待验证]** |
| USB 拔出 | 全部资源释放 | APP 最终 stopped，无 ASSERT、复位或野指针 | **[APP/硬件待验证]** |
| BLE 断连/CCC off | 在线目的地丢失 | 立即停止录音，USB 通话不受影响 | **[APP/硬件待验证]** |

### 15.2 格式矩阵

**[APP/硬件待验证]** 至少覆盖：

| PC Speaker | PC MIC | 录音输出 |
| --- | --- | --- |
| 48 kHz / 24 bit / 2 ch | 48 kHz / 16 bit / 2 ch | 16 kHz / mono / 16 bit -> Opus |
| 48 kHz / 16 bit / 2 ch | 16 kHz / 16 bit / 2 ch | 同上 |
| 44.1 kHz / 16 bit / 2 ch | 44.1 kHz / 16 bit / 2 ch | 同上 |
| 16 kHz / 16 bit / 1或2 ch | 16 kHz / 16 bit / 1或2 ch | 同上 |
| 运行中切换支持格式 | 运行中切换支持格式 | 当前录音终止；新格式稳定后可重新 START |

### 15.3 音频质量

**[APP/硬件待验证]** 验收项：

- Near/Far/Mix PCM 无持续全零、样本错位、周期重复和位宽解释错误；
- 1 kHz 标准音的频率、幅度和 SRC 比例正确；
- Mixer clipping count 为 0，或有明确可解释的极端用例；
- 双讲时近端与远端都可懂；
- 录音中无明显 USB 包周期噪声、20 ms 边界爆音和 Resume 旧音；
- Near/Far 相对时延在产品门限内，且 2 小时内不持续漂移；
- APP 播放与主机离线 Opus 解码结果一致。

### 15.4 稳定性和资源

**[APP/硬件待验证]** 必须完成：

- 连续通话录音 2 小时；
- START/STOP 100 次；
- PAUSE/RESUME 50 次；
- USB Speaker/MIC 分别 close/open 50 次；
- USB 格式切换 30 次；
- BLE 断连重连 30 次；
- 最坏组合下 heap、物理内存、最大连续块和 CPU 不持续恶化；
- USB Speaker/MIC 无掉音，HID 正常，无 USB 重枚举；当前无 CVP 基线不检查算法 frame size，后续恢复算法时必须补回 CVP/AEC 专项检查；
- `produced/enqueued/sent/drop`、PCM in/out、underflow/overflow 可以完整对账。

### 15.5 构建与生成物

**[推荐设计]** 实施完成后必须：

- RDX ON 干净构建；
- RDX OFF 干净构建；
- 修改 source-selection 宏或流程资源后先 clean 再 all；
- 记录 `stream.bin`、固件、Map 的大小和哈希；
- 确认 `output/` 生成新固件；
- 由 JL 工具修改并重新导出音频流程，不手改 `.x6flow` 或生成的 `jlstream_node_cfg.h`；
- 不把目标编译成功描述为 USB/RDX 硬件行为已经验证。

## 16. 风险与未决问题

| 风险/问题 | 等级 | 当前结论 | 标签 |
| --- | --- | --- | --- |
| JL Mixer 是否支持跨顶层 Pipeline 汇聚 | 高 | 第一项 Spike；失败即转有界 Bridge | **[Spike 假设]** |
| `pc_spk` 位于 `0xD96F`，不在 USB `0xDC8D` | 高 | 仅改 USB Audio 图拿不到远端 | **[已确认事实]** |
| 两个 ADC Pipeline 破坏 CVP | 高 | 通话录音禁止新开 ADC | **[已确认事实]** |
| CVP/ANS 与 Bridge/Opus 并行时资源余量不足 | 高 | 当前以通话录音业务为先，暂时关闭单 MIC CVP/ANS；后续独立优化并在完整资源与听感回归后恢复 | **[已确认事实]** |
| APP scene `1` 是否接受 mono Opus | 高 | 先抓 Golden Trace，不提前开 Ability | **[APP/硬件待验证]** |
| Tap 在 `Vol_PcMic` 前绕过 MIC mute | 高 | 默认建议服从 mute，但需产品冻结 | **[APP/硬件待验证]** |
| 近端/远端时钟和帧长不同 | 高 | 两路 SRC + 有界缓冲 + 漂移统计 | **[推荐设计]** |
| Mixer 反压 USB 主链路 | 高 | USB 优先；录音丢弃或终止，不得阻塞 | **[推荐设计]** |
| 混音削顶或双方响度差异 | 中高 | 每路先留约 6 dB headroom，实机收敛 | **[推荐设计]** |
| 路线 B 破坏 24-bit 播放 | 高 | A/C 均失败后才评审；必须独立音质与格式回归 | **[APP/硬件待验证]** |
| Source close 与 RDX STOP 竞态 | 高 | Engine 单任务拥有 Backend 生命周期，close 幂等 | **[推荐设计]** |
| Pause 后发送旧 PCM/Opus | 高 | 关 Gate、清队列、递增 `capture_generation` | **[推荐设计]** |
| USB 活跃不等于真实通话 | 中 | 只接受 APP 显式 scene `1` | **[推荐设计]** |
| 原生 `mix_record_api` 引入文件系统 | 中 | 只借架构模式，不启用文件业务 | **[推荐设计]** |
| stereo 与现有 80 B mono 合同混淆 | 高 | stereo 建独立 profile 和 APP 验收 | **[推荐设计]** |

**[APP/硬件待验证]** 实施前仍需产品负责人明确两个隐私问题：Host MIC mute 是否同时静音近端录音，以及 Host Speaker mute 是否静音远端录音。未冻结前不得进入量产验收。

## 17. 最终决策摘要

| 决策 | 冻结结果 | 标签 |
| --- | --- | --- |
| 首版目标 | USB 双向稳定时，近端 + 远端混为 mono 在线发送 | **[推荐设计]** |
| ADC 策略 | 附着 USB 已有近端 PCM，绝不再开 ADC/申请第二份租约 | **[推荐设计]** |
| Near Tap | 当前为 EQ 后、PCMIC PlaySync/Vol/Expander 前；后续恢复算法时为 CVP/EQ 后 | **[推荐设计]** |
| Far Tap | PCSPK PlaySync 后，播放 Vol/EQ 前 | **[推荐设计]** |
| 编码合同 | 16 kHz / mono / 32 kbps / 20 ms / raw Opus / 80 B | **[推荐设计]** |
| JL 复用 | 继续复用 Gate/Switch、Encoder、AI_TX；SRC 可在阶段 3 按听音替换；跨 Pipeline Mixer/`0x9795` 已失败退出 | **[推荐设计]** |
| JL 文件录音 | 不复用 `file_recorder`/WriteFile 业务层 | **[推荐设计]** |
| 实施路线 | A 原生跨流 -> C 有界 PCM Bridge；B 合并 Pipeline 仅保留评审 | **[推荐设计]** |
| 场景触发 | APP 显式 scene `1`；USB 活跃只做前置校验 | **[推荐设计]** |
| 源失效 | 任一路关闭、重启、改格式或 USB 拔出即终止当前录音 | **[推荐设计]** |
| Pause/Resume | USB 不停；Gate 暂停；Resume 新建 `capture_generation` | **[推荐设计]** |
| Ability | APP Golden Trace 和全部实机验收通过后再发布 Call Recording | **[推荐设计]** |
| stereo | 后续独立 profile，语义固定 `L=near / R=far` | **[推荐设计]** |
| 通话处理算法 | 当前因 CPU/RAM 余量不足暂时关闭单 MIC CVP/ANS，以通话录音业务稳定为先；后续作为独立优化项恢复 | **[已确认事实]** |

**[推荐设计]** 阶段 2A 回退和阶段 2C 有界 Bridge 短时验收均已通过，PCM 汇聚路线冻结为“现有 Near/Far Tap -> 有界 Bridge -> Near 样本主时钟 mono 混音”。阶段 3 已完成固定容量异步队列、标准 Encoder/AI_TX 实机对账和 Far 欠载连续性短时复测；当前继续推进离线 Opus 听音和 APP 数据闭环，通话处理算法待录音业务稳定后独立优化。APP scene `1` 合同、离线 Opus 解码和异常矩阵通过前，不得发布 Call Recording Ability。
