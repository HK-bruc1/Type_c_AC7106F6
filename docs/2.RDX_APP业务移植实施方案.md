# AC710N RDX APP 业务分阶段移植实施方案

> 文档状态：v1.6（第二批绑定、断连和冷启动已验证，VM 写失败待补测）
> 编写日期：2026-08-07
> 最后更新：2026-08-08
> 目标工程：AC710N / BR56 Type-C 有线耳机
> 实施基线：`a03e293`
> 前置文档：`docs/0.RDX_BLE_MVP0实施方案.md`
> 问题复盘：`docs/1.RDX_BLE与USB_Audio内存冲突问题复盘.md`
> 参考工程：`t2616cc-firmware/`（JL701N / BR28，仅用于确认协议行为和字段，不作为整体移植源）

## 1. 当前结论

MVP0 已完成 RDX 广播、GATT、Identity、Notify、AppKey 认证和最小 APP 握手，下一阶段应进入 APP 可见的设备业务移植。实施必须继续遵守“模块化、最小侵入 JL 原生平台”的原则，并按副作用从低到高分成三个独立批次：

1. 只读业务：具名能力位、真实能力值、查询设备授权与 SN、查询待机时间；
2. 绑定业务：RDX 独立状态存储、运行时 Bound 广播位、APP 绑定；
3. 状态清理业务：APP 解绑、恢复出厂、ACK 后仅断开 RDX BLE。

录音、文件、存储格式化、Wi-Fi、SPI、OTA、LED、马达、充电仓与耳机配对等外设业务不在本阶段范围内。特别是 `*APP#devpair#` / `*APP#devunpair#` 属于充电仓与耳机的外设配对，不得与 APP 绑定状态混为一谈。

本方案同时记录实施边界和验收合同。第一批只读业务已完成代码实施，但仍有目标 APP Golden Trace、广播实测和 USB 共存回归待补齐。第二批已完成代码实施，范围严格限定为 RDX 自有状态、运行时 Bound 和绑定命令；绑定、重复绑定、正常断连、冷启动持久化和 USB 共存已有实机日志证据，VM 写失败事务仍待补测，未完成前不作第二批完整签收。第三批仍保持“代码待实施”。

## 2. 实施目标与硬约束

### 2.1 目标

- 让目标 APP 在 MVP0 握手后读取真实、边界明确的设备管理信息；
- 以小步提交方式加入绑定、解绑和恢复出厂；
- 为量产身份和后续业务保留可替换的 Provider/Adapter 接口；
- 保持 USB Speaker、Mic、HID、CVP 和 RDX BLE 共存基线；
- 保持 RDX OFF 零源码、零符号、零 VM、零运行行为；
- 保持官方 `librdxApp.a` 只抽取 `rdx_encryption.c.o`。

### 2.2 不允许发生的事情

- 不整体移植 701 的 `rdx_app.c`、`rdx_vm.c` 或 `rdx_protocol.c.o`；
- 不引入 701 的录音、文件、Wi-Fi、TWS、经典蓝牙、OLED、存储格式化或电源副作用；
- 不在 USB、UAC、音频、PC 模式或 JL 核心模块中加入 RDX 业务判断；
- 不为每个业务批次增加平行的产品总开关；
- 量产配置不得用虚假能力位宣告未实现业务；当前开发阶段允许显式使用命令发现 Ability 组合打开 APP 页面，但必须与真实产品能力区分并在量产前重新收敛；
- 不通过整机 `cpu_reset()` 完成解绑或恢复出厂；
- 不清除 BLE MAC、AuthKey、Label SN、产品型号和其他量产身份；
- 不清除 JL 音频参数、USB 配置、校准数据和其他模块拥有的 VM；
- 不直接把 `flash_get_sn_data()` 当成 RDX 量产身份接口；
- 量产固件不得保留完整协议身份与密钥日志；当前开发调试阶段允许开发人员采集完整 AppKey、AuthKey、Label SN 和线上报文，量产前必须统一关闭。

## 3. 当前基线与业务缺口

### 3.1 当前代码已经具备

| 能力 | 当前实现 | 说明 |
| --- | --- | --- |
| BLE 广播与扫描响应 | 已有 | Manufacturer Data 中已有产品字段、Bound 字节和 4 B Ability |
| GATT Identity | 已有 | 当前为 95 B Charge Case 兼容格式 |
| Battery | 已有 | 固定开发值 100% |
| Version | 已有 | 使用兼容配置中的硬件/固件版本 |
| AppKey | 已有 | 成功后进入 `APP-ready`，失败后返回失败并断开 |
| RTC | 部分兼容 | 只校验并回显时间戳，不设置或持久化系统 RTC |
| OS Type | 已有 | 只做兼容 ACK |
| PING/PONG | 已有 | 用于通路诊断 |

### 3.2 当前缺口和风险

| 项目 | 当前状态 | 本方案处理 |
| --- | --- | --- |
| Device Ability | 开发命令发现组合 `0x00000EA2` | 广播输出 `A2 0E 00 00`；仅用于打开 APP 页面和发现 Wire，不表示业务已实现 |
| Device Base Info | Identity 已覆盖 | 第一批不重复建立第二套基础信息结构 |
| `authsn` | 第一批已实现 | AppKey 后才允许返回；目标 APP Golden Trace 待完成 |
| `cofftime` | 第一批已实现 | 返回真实禁用值 0，线上单位为分钟；目标 APP Golden Trace 待完成 |
| `offtime` 设置 | 未实现 | 当前 Type-C 自动关机被强制关闭，本阶段不实现 |
| Bound 运行时状态 | 第二批已实现 | RDX 独立版本化状态存储，广播读取当前 RAM 状态 |
| APP 绑定 | 第二批已实现 | 只改变 RDX Bound 状态，不触碰 BLE Bonding、USB 或音频 |
| APP 解绑 | 未实现 | 第三批实现，只清理 RDX 可变状态 |
| 恢复出厂 | 未实现 | 第三批实现，不清理其他模块和量产身份 |

### 3.3 当前兼容身份继续冻结

业务移植期间继续保持当前开发兼容身份：

```text
Local Name   = Zenchord Case 0000
Product Code = 603
Factory Code = ZENCHD
Product Type = E1
Self Mark    = NV
Role         = Charge Case compatibility
```

不得在同一批次同时切换正式 Type-C 产品身份。否则 APP 页面或连接行为变化时，无法判断是身份字段变化还是业务命令变化导致。

## 4. 协议事实与术语边界

### 4.1 设备能力是 32 bit 位域

参考工程使用能力位按位或组合，而不是把能力值当成无语义整数：

```c
#define RDX_DEVICE_ABILITY \
    (ABILITY_CONFERENCE_RECORDING | ABILITY_RTC)
```

当前 `0x22` 即 `BIT(5) | BIT(1)`。Manufacturer Data 按低字节到高字节依次写入 4 B：

```text
Byte 0 = ability[7:0]
Byte 1 = ability[15:8]
Byte 2 = ability[23:16]
Byte 3 = ability[31:24]
```

公共 RDX 协议头应定义已知功能位，产品配置只负责选择并按位或：

| 位 | 公共含义 | 当前产品 |
| ---: | --- | --- |
| 7 | Local Storage | 开发命令发现启用；业务未实现 |
| 6 | Wi-Fi AP | 不启用 |
| 5 | Conference Recording | 开发命令发现启用；业务未实现 |
| 4 | Call Recording | 不启用 |
| 3 | Conference Noise Reduction | 不启用 |
| 2 | Classic Bluetooth | 不启用 |
| 1 | RTC | 开发命令发现启用；当前回显不等于完整 RTC 能力 |
| 0 | Multimedia Recording | 不启用 |
| 15 | Left/Right 1V1 | 不启用 |
| 14 | Wi-Fi AP V2 | 不启用 |
| 13 | Child/Parent | 不启用 |
| 12 | Network | 不启用 |
| 11 | Flashnote | 开发命令发现启用；业务未实现 |
| 10 | Record Mark | 开发命令发现启用；业务未实现 |
| 9 | Record Pause/Resume | 开发命令发现启用；业务未实现 |

保留位保持 0。定义或广播能力位不等于对应业务已编译或实现；当前六个置位只用于目标 APP 命令发现，协议层仍只处理本批已经实现的命令。

### 4.2 三种“绑定”必须分开

| 名称 | 命令 | 含义 | 本阶段 |
| --- | --- | --- | --- |
| APP 绑定状态 | `*APP#bound#...` | APP 与当前 RDX 设备的业务绑定标志 | 第二批 |
| APP 主动解绑 | `*APP#unbound#...` 或 Bound 命令的解绑分支 | 清除 RDX 业务绑定状态 | 第三批 |
| 充电仓与耳机配对 | `*APP#devpair#...` / `*APP#devunpair#...` | 仓与外部耳机的 Auth/MAC/SN 配对 | 不移植 |

APP Bound 状态不是 BLE Security Manager Bonding，也不是经典蓝牙配对数据库。实现 APP 绑定不得调用 `USER_CTRL_DEL_ALL_REMOTE_INFO`、TWS 清配对或 JL Unicast 绑定接口。

### 4.3 协议事实的优先级

当参考头文件、701 实现和 APP 实际行为不一致时，按以下优先级冻结合同：

1. 目标 APP 的 Golden Trace；
2. 已确认的正式 RDX 协议说明；
3. 701 参考工程的实际线上实现；
4. 参考头文件中的注释和变量名。

待机时间就是已知反例：参考头文件把字段注释为秒，但 701 的 `sys_get_auto_off_time()` 和 `sys_set_auto_off_time()` 实际按分钟读写。因此本工程线上单位明确采用分钟，并在 Golden Trace 中逐字节确认。

## 5. 目标模块划分

### 5.1 依赖关系

```text
rdx_mvp0_compat_config.h
        │
        ├──────────────► rdx_protocol_defs.h
        │                       │
        ▼                       │
rdx_identity ───────────────────┤
        │                       │
        │ runtime Bound         ▼
        └──────────────► rdx_device_state
                                ▲
                                │
rdx_mvp0_protocol ─────► rdx_device_management
        │
        ├──────────────► rdx_identity
        ├──────────────► rdx_platform_br56
        └──────────────► Transport send/disconnect callbacks
```

`rdx_mvp0_protocol` 只拥有会话状态、命令识别、认证门控、报文编码和发送时序。它不得直接调用 `syscfg_read()` / `syscfg_write()`，也不得直接修改 USB、音频或 JL 全局状态。

### 5.2 文件职责

| 文件 | 阶段 | 职责 |
| --- | --- | --- |
| `rdx_protocol_defs.h` | 第一批新增 | 公共命令常量、Ability 位定义和与产品无关的线上字段定义 |
| `rdx_mvp0_compat_config.h` | 第一批修改 | 当前产品兼容身份、开发占位 AuthKey/Label SN、产品选择的 Ability 组合 |
| `rdx_identity.[ch]` | 第一批扩展 | BLE MAC、AuthKey、Label SN、Identity、Manufacturer Data；第二批读取运行时 Bound |
| `rdx_platform_br56.[ch]` | 第一批新增 | BR56 产品适配接口；当前待机时间查询返回禁用值 0 |
| `rdx_mvp0_protocol.[ch]` | 各批扩展 | 命令解析、`app_ready` 门控、线上应答、ACK 后动作调度 |
| `rdx_device_state.[ch]` | 第二批已新增 | RDX 可变状态默认值、校验、VM 读写和版本化兼容占位 |
| `rdx_device_management.[ch]` | 第二批已新增 | 绑定业务边界和幂等状态转换，不包含线上字符串；解绑、恢复出厂仍留在第三批 |
| `rdx_ble_transport_br56.[ch]` | 第三批小幅扩展 | 继续提供 send/disconnect；断开后从最新状态重建广播 |
| `rdx_protocol_entry.[ch]` | 原则上不改职责 | 只负责 RDX 模块生命周期 |

不新增公共的 `rdx_app.c`，也不恢复 701 的大一统业务入口。

### 5.3 构建门控

新增源码继续由 `SDK/build/genFileList.c` 在 `#if TCFG_RDX_ENABLE` 下统一注入。不得给 `rdx_device_state`、`rdx_device_management` 或 `rdx_platform_br56` 增加绕过最终门控的独立构建入口。

`TCFG_RDX_ENABLE=0` 时必须满足：

- 不编译上述新增源码；
- 不分配 RDX VM 状态；
- 不注册业务命令、定时器或断连动作；
- 不改变非 RDX 的 BLE、USB 和 RAM 配置。

## 6. 第一批：只读设备业务

### 6.1 实施范围

第一批只允许：

1. 将 Ability 魔数改为公共具名位组合；
2. 使用具名位组合配置当前 Ability；第一批提交时为 `RDX_ABILITY_NONE`，后续开发命令发现阶段临时启用六个目标位；
3. 增加 `*APP#authsn#` 查询和应答；
4. 增加 `*APP#cofftime#` 查询和应答；
5. 增加只读 Provider/Adapter 接口；
6. 增加结构化开发调试日志，完整记录 APP 写入、设备上报和认证字段，量产前统一关闭敏感报文输出。

第一批不得写 VM、改变 Bound、重启、断开 BLE 或修改 USB 状态。

### 6.2 Ability 实现

公共头定义能力常量，产品配置使用：

```c
#define RDX_COMPAT_DEVICE_ABILITY                        \
    (RDX_ABILITY_RTC                                   | \
     RDX_ABILITY_CONFERENCE_RECORDING                  | \
     RDX_ABILITY_LOCAL_STORAGE                         | \
     RDX_ABILITY_RECORD_PAUSE_RESUME                   | \
     RDX_ABILITY_RECORD_MARK                           | \
     RDX_ABILITY_FLASHNOTE)
```

当前组合值为 `0x00000EA2`，Manufacturer Data 小端字节为 `A2 0E 00 00`。该值是开发命令发现配置，用于显示 APP 页面并采集完整 Wire；不得把置位本身写成录音、存储、暂停、打标、闪记或完整 RTC 已实现。

`rdx_identity_fill_manufacturer_data()` 继续按 4 B 小端字节顺序写入。除 Ability 四个字节外，Product Code、MAC、Factory Code、Bound 字节、Protocol Version、Product Type 和 Self Mark 必须保持当前顺序和值不变。

后续将任何能力位确认为正式产品能力必须同时满足：

1. 对应业务已经实现；
2. APP 线上命令已经逐字节验证；
3. 构建、RAM 和实机验收已完成；
4. 本文档和 0 号 MVP 文档已更新。

### 6.3 `authsn` 查询

请求前缀已由参考协议确认：

```text
*APP#authsn#
```

目标应答字段按参考 `DevBaseInfo` 和协议上行前缀组织：

```text
*DEV#authsn#<AuthKey>#<BLE_MAC>#<WiFi_MAC>#<Label_SN>#
```

已从 701 参考库 `rdx_protocol.c.o` 的 LLVM bitcode 确认实际编码为 `%s#%s#%s#%s#`，第一批按该格式实现。仍必须用目标 APP Golden Trace 逐字节确认字段顺序、大小写和结束符，未通过前不能视为正式量产合同。当前开发数据源如下：

| 字段 | 当前数据源 | 当前值/行为 |
| --- | --- | --- |
| AuthKey | `rdx_mvp0_compat_config.h` | 24 个 ASCII `0`，仅开发占位 |
| BLE MAC | Controller | 真实 6 B MAC，格式化为 12 个大写十六进制字符 |
| Wi-Fi MAC | 无 | 12 个 ASCII `0`；Wi-Fi 不在本阶段范围 |
| Label SN | `rdx_mvp0_compat_config.h` | 16 个 ASCII `0`，仅开发占位 |

约束：

- 仅在 `app_ready=1` 时响应；
- AppKey 前收到请求时只记录拒绝原因，不回包、不主动断开；
- 不调用 `flash_get_sn_data()`；
- 当前开发调试日志允许输出完整 AuthKey 和 Label SN，以便与 APP 报文逐字节核对；量产前必须关闭；
- 量产烧录格式确定后只替换 Identity Provider，不修改协议层报文逻辑。

### 6.4 `cofftime` 查询

请求前缀已由参考协议确认：

```text
*APP#cofftime#
```

参考回调签名为 `offtime_check_ack_indicate(result, value)`。已从 701 参考库 LLVM bitcode 确认实际编码为 `%d#%u#`，第一批按以下形式实现；目标 APP Golden Trace 仍需确认：

```text
*DEV#cofftime#<result>#<minutes>#
```

当前产品结果：

```text
result  = 0
minutes = 0
```

`0` 表示当前 Type-C 产品未启用自动关机。不得返回参考工程的开发默认值，也不得为了让 APP 页面显示正常而伪造非零时间。

本批次不识别或不成功处理 `*APP#offtime#...` 设置命令。只有产品明确要求 USB 供电期间允许自动关机，并完成 USB 行为评审后，才能单独设计该功能。

### 6.5 认证前后命令矩阵

| 命令 | AppKey 前 | `APP-ready` 后 |
| --- | --- | --- |
| PING/PONG | 保持现有行为 | 允许 |
| Battery | 保持现有行为 | 允许 |
| Version | 保持现有行为 | 允许 |
| AppKey | 允许 | 按现有行为处理 |
| RTC | 保持现有兼容行为 | 允许 |
| OS Type | 保持现有兼容行为 | 允许 |
| `authsn` | 忽略并记录拒绝 | 允许 |
| `cofftime` | 忽略并记录拒绝 | 允许 |
| 未知业务命令 | 忽略 | 忽略 |

第一批不得借机改变 MVP0 已验证的握手顺序。

### 6.6 第一批实际修改清单

```text
新增  SDK/apps/common/third_party_profile/rdx_protocol/rdx_protocol_defs.h
新增  SDK/apps/common/third_party_profile/rdx_protocol/rdx_platform_br56.c
新增  SDK/apps/common/third_party_profile/rdx_protocol/rdx_platform_br56.h
修改  SDK/apps/earphone/include/rdx_mvp0_compat_config.h
修改  SDK/apps/common/third_party_profile/rdx_protocol/rdx_identity.c
修改  SDK/apps/common/third_party_profile/rdx_protocol/rdx_identity.h
修改  SDK/apps/common/third_party_profile/rdx_protocol/rdx_mvp0_protocol.c
修改  SDK/build/genFileList.c
更新  本文档状态与验证记录
```

若实现中发现只为返回常量 0 而新增平台源文件会造成不必要复杂度，可以保留统一 Adapter API 并在相邻职责明确的位置实现，但不得把待机产品策略塞入 BLE Transport。

## 7. 第二批：APP 绑定与运行时 Bound

### 7.1 进入条件

第二批代码实施前核对了以下静态和协议边界；第一批仍有部分实机项目待补测，故本批只提交低副作用代码，不开放第三批动作：

- `authsn` 和 `cofftime` Golden Trace 通过；
- Ability 和其他扫描响应字段逐字节核对通过；
- MVP0 握手与 USB Speaker 共存无回归；
- 干净构建、Map 和 RDX OFF 检查通过。

### 7.2 RDX 状态结构

新增单一、版本化的状态结构：

```c
#define RDX_DEVICE_STATE_VERSION    1

typedef struct {
    u8 version;
    u8 bound;
    u8 reserved[2];
} rdx_device_state_t;
```

状态模块负责：

- 初始化默认值；
- 读取 VM；
- 校验读取长度、版本和 `bound` 合法性；
- 写入成功后更新 RAM；
- 提供只读快照；
- 恢复出厂时回到默认值；
- 屏蔽 `syscfg_read()` / `syscfg_write()` 细节。

当前产品默认值为未绑定。读取失败、长度不符、版本不匹配或 `bound > 1` 时，RAM 回退为默认未绑定并记录日志；不要在每次异常启动时立即反复写 VM，等首次合法状态变更时再落盘。

### 7.3 VM ID 规划

RDX 状态必须使用当前产品 `user_cfg_id.h` 中的用户自定义 VM ID，不复用 701 的 `VM_RDX_NOTTA_BOUND_STATUS=149`，因为当前 BR56 SDK 的 149 已被闹钟配置占用。

当前产品最终使用用户自定义区中的 ID 15：

```c
#define CFG_RDX_DEVICE_STATE    15
```

原建议 ID 14 未采用：它与未启用的公共模块配置区存在潜在占用，不能作为本产品长期 VM 合同。ID 15 已完成全仓数值核对，未发现当前启用配置冲突。不得使用 50～99 的 SDK VM 保留区，也不得直接复制 701 的 RDX VM 编号。

### 7.4 写入事务语义

```text
读取旧状态
    │
    ├─ 目标值与旧值相同 ──► 幂等成功，不写 VM
    │
    └─ 目标值不同
            │
            ├─ syscfg_write 失败 ──► RAM 不变，返回失败
            │
            └─ syscfg_write 成功 ──► 更新 RAM，返回成功
```

协议层只能在状态模块返回成功后发送成功 ACK。若状态已经成功落盘但 ACK 发送失败，不做第二次写入回滚；断开后 APP 通过重连和幂等命令恢复一致性。

### 7.5 运行时 Bound 广播位

当前 `RDX_COMPAT_BOUND_STATE` 只保留为首次无有效 VM 时的默认值。第二批后，Manufacturer Data 的 Bound 位必须来自运行时状态：

```text
bound == 0 -> mode/bound byte 的 BIT(6) 为 0
bound == 1 -> mode/bound byte 的 BIT(6) 为 1
```

RDX 初始化顺序调整为：

```text
rdx_device_state_init
    └── rdx_identity_init
            └── rdx_ble_transport_init
                    └── build advertising data
```

绑定发生在连接期间时无需立即强制断连。当前连接正常结束后，Transport 恢复广播时会重新调用 `rdx_identity_fill_manufacturer_data()`，自然带上最新 Bound 位。

### 7.6 APP 绑定行为

参考协议使用 `*APP#bound#...`。从参考工程线上实现确认，本批请求只有一个十进制 `cmd` 参数；当前只接受以下请求：

```text
*APP#bound#1#
```

成功响应为：

```text
*DEV#bound#0#1#
```

其中第一个字段是结果码（`0` 成功、`1` 失败），第二个字段是当前持久化 Bound 值。第二批只开放“设置为已绑定”的分支：

```text
未认证        -> 忽略并记录
参数非法      -> 忽略并记录，不改变状态
已经绑定      -> 幂等成功，不写 VM，不断开
尚未绑定      -> 先成功落盘，再返回成功 ACK
ACK 发送失败  -> 保留已落盘状态，等待 APP 重连确认
```

`*APP#bound#0#` 当前作为未开放的解绑分支处理：记录 `unbind_deferred`，返回失败响应 `*DEV#bound#1#<current_bound>#`，不改变状态。未认证请求、长度错误、缺少结尾 `#` 或非 `0/1` 参数均不回包、不改变状态。解绑统一留到第三批完成完整时序后开放。

## 8. 第三批：解绑与恢复出厂

### 8.1 进入条件

第二批必须先完成：

- 绑定、正常断连、广播 Bound=1；
- 冷启动后 Bound=1；
- 重复绑定不重复写 VM；
- USB Speaker 活跃期间绑定不影响播放和枚举。

### 8.2 当前产品允许清理的内容

| 数据 | 解绑 | 恢复出厂 | 说明 |
| --- | ---: | ---: | --- |
| RDX Bound | 清除 | 清除 | RDX 自有可变状态 |
| 未来 RDX 用户设置 | 按协议决定 | 清除 | 必须仍属于 RDX 状态结构 |
| 当前 `app_ready` | 会话结束时清除 | 会话结束时清除 | RAM 会话态 |
| BLE MAC | 保留 | 保留 | 量产身份 |
| AuthKey | 保留 | 保留 | 量产身份/密钥 |
| Label SN | 保留 | 保留 | 量产身份 |
| Product Code/Type | 保留 | 保留 | 产品身份 |
| USB/UAC/HID 配置 | 保留 | 保留 | 非 RDX 所有 |
| JL 音频/EQ/CVP 参数 | 保留 | 保留 | 非 RDX 所有 |
| TWS/经典蓝牙数据库 | 不操作 | 不操作 | 当前产品不拥有对应 RDX 业务 |
| 存储介质 | 不格式化 | 不格式化 | Local Storage Ability 未启用 |

### 8.3 解绑时序

```text
收到解绑命令
    │
    ├─ app_ready == 0 ──► 忽略并记录
    ├─ transition_pending == 1 ──► 拒绝新的状态修改
    └─ 参数合法
            │
            ├─ 已经未绑定 ──► 幂等成功，不重复写 VM
            └─ 当前已绑定
                    │
                    ├─ VM 写失败 ──► 失败 ACK，保持连接和原状态
                    └─ VM 写成功
                            │
                            ├─ 发送成功 ACK
                            ├─ 标记 transition_pending
                            ├─ 延迟约 100 ms
                            └─ 只断开 RDX BLE
                                    └─ 以 Bound=0 恢复广播
```

如果状态已经成功清除但 ACK 无法发送，仍应断开 RDX BLE并以未绑定状态恢复广播；APP 可重连后再次发送幂等解绑确认最终状态。

参考 `unbound` 命令中的 `user_para` 和 `format_en` 可以为兼容目的解析。即使当前开发命令发现广播临时置位 Local Storage，存储业务仍未实现，不得因 `format_en=1` 格式化任何存储；`user_para` 也不得触发清经典蓝牙、改名、改 Mic Gain 或其他 701 副作用。

### 8.4 恢复出厂时序

`*APP#default#` 只恢复 RDX 自有可变状态：

1. 必须已进入 `APP-ready`；
2. 检查没有其他状态转换正在等待；
3. 将 RDX 状态恢复为默认未绑定；
4. 状态落盘成功后发送成功 ACK；
5. ACK 成功入队后延迟约 100 ms；
6. 结束当前 RDX BLE 会话；
7. 以默认未绑定状态恢复 RDX 广播；
8. USB Audio 持续工作，不执行 `cpu_reset()`。

即使当前状态已经是默认值，恢复出厂也应成功 ACK，并结束当前 RDX 会话，使 APP 观察到一次完整的恢复流程。

### 8.5 延迟动作生命周期

协议状态增加：

```text
state_transition_pending
post_ack_action
post_ack_timer_id
```

约束：

- 同一时刻最多一个状态转换；
- 等待断开期间拒绝新的绑定、解绑和恢复出厂；
- 连接提前断开时取消定时器并清状态；
- `rdx_mvp0_protocol_exit()` 必须取消定时器；
- RDX 停止后回调不得再访问已释放的 BLE Handle；
- 不使用阻塞 `os_time_dly()` 等待 ACK。

## 9. 报文与错误处理合同

### 9.1 通用解析规则

- 固定查询命令必须长度完全相等；
- 带参数命令必须检查前缀、字段数量、字段范围和结尾 `#`；
- 不对非 NUL 终止的 ATT Payload 直接调用无界字符串函数；
- 所有输出缓冲必须静态定长或有明确上限；
- 超出单次 ATT 有效 Payload 时返回失败，不做隐式分片，除非后续协议明确要求；
- 未知命令保持忽略，不导入 701 的全量命令解析器。

### 9.2 失败语义

| 场景 | 行为 |
| --- | --- |
| 未认证的只读管理命令 | 忽略并记录，不返回敏感信息，不主动断开 |
| 未认证的状态修改命令 | 忽略并记录 |
| AppKey 校验失败 | 保持 MVP0 现有行为：失败 ACK 后断开 |
| 报文格式错误 | 按 Golden Trace 决定失败 ACK 或忽略；不得越界解析 |
| VM 读取异常 | 默认未绑定，记录原因 |
| VM 写入失败 | RAM 和广播状态不变，失败 ACK |
| ACK 发送失败 | 不伪造成功日志；已落盘状态不回滚，通过重连和幂等命令恢复 |
| 状态转换进行中 | 拒绝新的状态修改，不覆盖 pending action |
| RDX 提前停止 | 取消定时器和异步动作，不触发迟到断连 |

### 9.3 日志要求

开发调试日志统一使用 `[RDX][RX]`、`[RDX][TX]`、`[RDX][DROP]`、`[RDX][AUTH]` 和 `[RDX][SESSION]` 分类，记录：

- APP 写入的完整 ASCII Wire；
- AppKey 的完整 32 B Wire Payload、解密后的 16 B AppKey 明文及十六进制值；
- 设备上报的完整 ASCII Wire、长度和发送返回值；
- 是否因 `app_ready=0` 被拒绝；
- 状态旧值、目标值和 VM 返回值；
- pending action 创建、取消和执行；
- 广播中最终 Bound 与 Ability 数值；
- ACK 发送返回值。

这些完整字段日志只适用于开发人员调试采集。量产固件必须关闭 AppKey、AuthKey、Label SN、未来量产授权数据和完整身份报文输出；不得把当前开发调试策略直接带入量产版本。

## 10. 分批提交与回退

### 10.1 建议提交边界

| 批次 | 只包含 | 不得夹带 |
| --- | --- | --- |
| 第一批 | Ability、`authsn`、`cofftime`、只读 Provider/Adapter、文档验证记录 | VM、绑定、复位 |
| 第二批 | RDX 状态存储、运行时 Bound、绑定命令 | 解绑、恢复出厂、外设 |
| 第三批 | 解绑、恢复出厂、ACK 后断连、pending action | 录音、文件、Wi-Fi、OTA |

每批都使用独立提交，不通过在同一提交中临时注释下一批代码形成“分批”。

### 10.2 回退方式

- 功能级回退：回退对应批次的独立提交；
- 产品级关闭：通过最终配置关闭 RDX，然后重新生成配置并干净构建；
- 不新增 `RDX_APP_BUSINESS_ENABLE`、`RDX_BIND_ENABLE` 等长期平行总开关；
- 不通过删除单个源文件或注释生命周期调用制造半关闭状态。

## 11. 构建与静态验证

每个批次至少执行一次干净构建：

```powershell
Set-Location SDK
$env:Path = "$PWD\tools\utils;$env:Path"
make clean
make
```

检查项：

1. `build/fileList.mk` 只增加本批次计划内的 RDX 源文件；
2. `cpu/br56/tools/sdk.map` 仍只从 `librdxApp.a` 抽取 `rdx_encryption.c.o`；
3. 不出现 `rdx_protocol.c.o`、`rdx_queue.c.o`、`rdx_uxfile.c.o` 和 `xxpUart.c.o`；
4. GATT Profile 仍为 387 B，Handle、UUID 和属性顺序不变；
5. Manufacturer Data 除计划中的 Ability/Bound 字节外逐字节不变；
6. RDX ON 的 Controller、GATT、ATT RAM 合同不被放大；
7. RDX OFF 时无新增源码、符号、VM、定时器和日志；
8. `git diff --check` 通过；
9. 固件大小、SHA-256 和 Map 差异写回本文档；
10. 工作区不混入参考工程、日志副本和构建生成物。

## 12. 第一批验收矩阵

### 12.1 协议验收

| 场景 | 预期 |
| --- | --- |
| AppKey 前发送 `authsn` | 无敏感回包，有明确拒绝日志，连接保持 |
| AppKey 前发送 `cofftime` | 无业务回包，有明确拒绝日志，连接保持 |
| AppKey 成功后发送 `authsn` | 报文与 Golden Trace 逐字节一致 |
| AppKey 成功后发送 `cofftime` | 返回成功和 0 分钟，与 Golden Trace 一致 |
| 发送畸形 `authsn/cofftime` | 不越界、不崩溃、不泄露数据 |
| 发送未知 701 业务命令 | 不响应，不引入全量业务 |
| 断开后重连 | `app_ready` 清零，必须重新 AppKey 才能查询管理信息 |

### 12.2 广播和身份验收

- 当前开发命令发现固件的 Ability 四字节为 `A2 0E 00 00`；
- 未绑定状态的 Bound 字节为 `00`，绑定状态的运行时 Bound 字节为 `40`（BIT(6)）；
- 其他 Manufacturer Data 字节与 MVP0 基线一致；
- GATT Identity 仍保持当前 95 B 开发兼容格式；
- BLE MAC 与 Controller 一致；
- AuthKey、Wi-Fi MAC 和 Label SN 明确为开发占位，不得写成量产已完成。

### 12.3 USB 共存验收

- USB Speaker 播放期间完成 AppKey、`authsn` 和 `cofftime`；
- 无 `MMASSERT`、`vmalloc_ 147`、异常复位或 USB 重枚举；
- CPU 和内存水位无异常持续上升；
- 能覆盖时补做 USB Mic/CVP 与 HID；无法覆盖的项目标为“未验证”。

## 13. 第二、三批验收矩阵

### 13.1 绑定

1. 未绑定冷启动，广播 Bound=0；
2. AppKey 后绑定成功；
3. 当前连接正常断开后，恢复广播 Bound=1；
4. 冷启动后仍为 Bound=1；
5. 重复绑定成功但不重复写 VM；
6. 绑定期间 USB Speaker 不断音、不重枚举；
7. VM 写失败时状态和广播保持原值，并返回失败。

### 13.2 解绑

1. 已绑定状态下解绑，APP 能收到成功 ACK；
2. ACK 后只断开 BLE，USB 不重枚举；
3. 恢复广播 Bound=0；
4. 冷启动后仍为 Bound=0；
5. 重复解绑幂等成功，不重复写 VM；
6. `format_en` 不触发任何存储格式化；
7. 等待断连期间的第二条状态修改命令被拒绝；
8. RDX 停止或连接提前断开后没有迟到定时器回调。

### 13.3 恢复出厂

1. 成功 ACK 后结束当前 RDX BLE 会话；
2. 以默认未绑定状态恢复广播；
3. BLE MAC、AuthKey、Label SN 和产品身份保持不变；
4. USB Audio、HID、JL 音频参数和校准数据保持不变；
5. 不调用 `cpu_reset()`，不出现 USB 重枚举；
6. Speaker 活跃期间执行恢复出厂不导致持续断音或崩溃。

## 14. 完整稳定性回归

三个批次完成后，在 0 号 MVP 剩余回归项基础上增加：

1. 连续 20 次 AppKey + `authsn/cofftime`；
2. 连续 20 次绑定/正常断开/重连；
3. 连续 20 次绑定/解绑；
4. 连续 10 次绑定后冷启动，状态均保持；
5. 连续 10 次恢复出厂，USB 均不重枚举；
6. Speaker、Mic、HID 与 RDX 管理命令同时运行 30 分钟；
7. APP 在线至少 10 分钟并反复查询只读信息；
8. 全程无堆下降、VM 写异常、悬空定时器、异常复位和广播状态漂移；
9. 至少两种目标手机/系统版本验证 APP 页面与重连行为。

当前板卡或测试条件无法覆盖的项目只能记录为“未验证”，不能写成“已通过”或“失败”。

## 15. 量产前阻塞项

以下事项不阻塞开发兼容业务移植，但阻塞量产签署：

1. 正式 Type-C 产品在 APP 中的产品形态；
2. 正式 Local Name、Product Code、Factory Code、Product Type 和 Ability；
3. 唯一 BLE MAC、AuthKey、Label SN 的烧录格式和授权流程；
4. AppKey 的量产管理、轮换和撤销策略；
5. `authsn` 是否允许返回 AuthKey，以及对应安全审查；
6. BLE Security Manager、Bonding 和 Secure Connections 最终策略；
7. 待机/自动关机是否对 USB 有线产品具有真实需求；
8. VM 状态结构的向后兼容和固件升级策略；
9. 多手机、系统版本和 APP 版本的 Golden Trace；
10. 0 号 MVP 与本文全部实机稳定性项目。

## 16. 文档更新规则

每完成一个批次，应同步更新：

- 文档状态和最后更新日期；
- 实际修改文件清单；
- 最终线上报文字节；
- Ability/Bound 的实际广播字节；
- VM ID 和状态结构最终定义；
- 构建结果、固件大小、SHA-256 和 Map 抽取；
- 实机日志、设备、系统和 APP 版本；
- 已通过、未通过和未验证项目。

任何实现若偏离本文约束，必须先修改并重新评审本文档，不能在代码中静默改变绑定、恢复出厂、量产身份或 USB 生命周期语义。

## 17. 第一批实施记录

### 17.1 代码结果

- Ability 已使用公共具名位定义；第一批提交配置为 `RDX_ABILITY_NONE`，后续开发命令发现配置改为 RTC、Conference Recording、Local Storage、Record Pause/Resume、Record Mark 和 Flashnote 的组合 `0x00000EA2`，Manufacturer Data 为 `A2 0E 00 00`；
- `authsn` 使用 Identity Provider 读取开发占位 AuthKey、Controller 真实 BLE MAC、占位 Wi-Fi MAC 和 Label SN；BLE MAC 按 6 B Controller 地址逆序格式化为 12 个大写十六进制字符；
- `cofftime` 通过 BR56 只读 Adapter 返回 `result=0`、`minutes=0`，没有调用或修改 JL 自动关机状态；
- 两条查询都要求固定命令完全匹配，并受 `app_ready` 门控；认证前只记录拒绝，不回包、不主动断连；
- 协议日志已结构化为 RX/TX/DROP/AUTH/SESSION；开发阶段完整打印 ASCII Wire、AppKey Wire Payload、解密 AppKey、AuthKey 和 Label SN，便于直接审查请求与应答；
- 第一批没有 VM 读写、定时器、复位、BLE 主动断连或 USB/音频副作用；
- 未增加第一批独立总开关，新增源文件继续统一受 `TCFG_RDX_ENABLE` 最终门控。

当前开发报文为：

```text
*DEV#authsn#000000000000000000000000#<Controller BLE MAC, 12 uppercase hex>#000000000000#0000000000000000#
*DEV#cofftime#0#0#
```

其中 `authsn` 固定长度为 80 B，`cofftime` 固定长度为 18 B。协议层不做隐式分片；若协商后的 ATT 有效 Payload 不足，Transport 返回发送失败并记录返回值。

### 17.2 2026-08-07 构建与静态验收

| 检查项 | 结果 |
| --- | --- |
| Windows 干净构建 | 通过；`make clean` + `make` 成功；本次后处理显示 `Device Offline`，最新版未自动下载到板 |
| GATT Profile | `0x183 = 387 B`，未修改 Profile 源码、Handle、UUID 或属性顺序 |
| 官方库抽取 | 仅 `librdxApp.a(rdx_encryption.c.o)` |
| 禁止对象 | 未抽取 `rdx_protocol.c.o`、`rdx_queue.c.o`、`rdx_uxfile.c.o`、`xxpUart.c.o` |
| 官方库 SHA-256 | `4289EC0F6D923EC9337A5DBE57F8D720BCC4946601B7D8393F9E2878B16F5C7D`，未变化 |
| RDX OFF | 完整 ELF 验证通过：文件清单、对象目录、Map 和 ELF 符号均无 RDX 残留 |
| 第一批源码与本文档 `git diff --check` | 通过；用户已暂存的原始日志仍保留其采集时尾随空格，不在本批修改范围 |
| 固件 | `output/update.ufw`，1,468,672 B |
| 固件 SHA-256 | `01E4651A5A050CF3F75475B9FCF09468E1E6A8EE88760D38C8CEE27742C9160D` |

相对 MVP0 基线固件 1,466,368 B，包含完整结构化开发日志的第一批固件增加 2,304 B；`.text` 相对日志优化前增加约 1.5 KB。此前第一批功能固件曾由 Windows `make` 自动下载到板，本次结构化日志最新版构建时设备离线，因此只生成 `output/update.ufw`，回归前需重新下载。临时 RDX OFF 验证只链接 ELF，没有执行后处理或下载，验证结束后已恢复正式 RDX ON 配置和 Map；本次日志优化仍沿用相同最终门控，未增加新的构建入口。

### 17.3 2026-08-07 实机验证记录

测试日志：

- `COM3_2026-08-07_20-41-46.log`：完整结构化协议链路和 USB Speaker 单轮共存；
- `COM3_2026-08-07_20-51-05.log`：BLE 断开、恢复广播、重连、重新认证，以及目标 APP 全部可用按键遍历。

| 项目 | 结果 | 证据与结论 |
| --- | --- | --- |
| 广播 Ability（第一批 NONE 基线） | 通过 | 历史扫描响应为 `ZENCHD 00 00 00 00 00 18 E1 NV`，Bound=0、Ability=`00 00 00 00`，其他身份字段不变 |
| 广播 Ability（当前命令发现配置） | 待实测 | 预期为 `ZENCHD 00 A2 0E 00 00 18 E1 NV`，只允许 Ability 四字节变化 |
| AppKey | 通过 | 完整 Wire Payload 可见，解密 AppKey 为 `7C0BAF778B727175`，成功 ACK 后进入 `app_ready=1` |
| `authsn` | 通过 | APP 写入与设备 80 B 上报完整可见，BLE MAC=`080504030201`，发送 `ret=0` |
| `cofftime` | 通过 | APP 写入与 `*DEV#cofftime#0#0#` 18 B 上报完整可见，发送 `ret=0` |
| 断开重连会话清零 | 通过 | 断开和下一次连接均记录 `app_ready=0`，随后重新执行 AppKey，不继承旧会话认证状态 |
| USB Speaker 单轮共存 | 通过 | Speaker 打开后完成 BLE、AppKey、`authsn/cofftime`；期间无 USB reset、重枚举、异常复位、`MMASSERT` 或 `vmalloc_ 147` |
| 目标 APP 全按键遍历 | 部分通过 | 已实现命令正常；另有 9 组按键操作因无 ACK 各重试 4 次并进入 Unknown，需取得完整 Wire 后划分为后续批次或明确暂缓业务 |

最后一项暴露出 Unknown 日志只打印长度和类型、无法直接映射 APP 按键的问题。本批已把 Unknown 调试日志补为完整 `wire=`、长度和类型；该改动只增强可观测性，不识别新命令、不改变第一批业务边界。后续再次遍历按键后，应建立“APP 按键 → Wire 命令 → 所属批次 → 当前处理策略”映射，不得因 APP 存在入口就直接导入参考工程副作用。

### 17.4 进入第二批前仍需完成

- 目标 APP 中确认 AppKey 前 `authsn/cofftime` 均无回包且连接保持；
- 使用补齐后的 Unknown `wire=` 日志再次遍历全部可用按键，完成命令归类；
- Speaker 播放期间连续执行至少 20 次 `authsn/cofftime`，确认无堆水位持续下降、断音、复位或 USB 重枚举；
- 完成目标 APP 在线至少 10 分钟的查询与重连稳定性验证；
- 条件允许时补测 USB Mic/CVP 与 HID；未覆盖项继续标记为“未验证”。

上述项目仍是第二批实机验收的前置遗留项；本批代码已在不引入第三方副作用的前提下实施，实机结果必须单独记录为“已通过”或“未验证”。

## 18. 第二批实施记录

### 18.1 代码结果

- 新增 `rdx_device_state.[ch]`，状态结构固定为 4 B：`version=1`、`bound`、`reserved[2]`；默认状态为未绑定；VM 读取失败、长度不符、版本不匹配或 `bound > 1` 时回退默认值，不在启动阶段反复写 VM；
- 新增 `rdx_device_management.[ch]`，协议层通过业务边界调用绑定操作，不直接访问 `syscfg_read()` / `syscfg_write()`；
- 最终 VM ID 为 `CFG_RDX_DEVICE_STATE=15`。ID 14 因未启用公共模块配置区的潜在占用而放弃；本产品当前启用配置未发现 ID 15 冲突；
- 绑定写入遵循事务语义：相同值幂等成功且跳过 VM；目标值变化时先写 VM，写失败保持 RAM 和广播原值，写成功后才更新 RAM；
- `rdx_identity_fill_manufacturer_data()` 的 Bound 字节改为读取运行时状态：未绑定为 `0x00`，已绑定为 `0x40`（BIT(6)）；其他 Manufacturer Data 字节和顺序不变；
- 初始化顺序调整为 `rdx_device_state_init` → `rdx_identity_init` → `rdx_ble_transport_init`，Transport 不再重复初始化 Identity；
- 新增并只接受 `*APP#bound#1#`。成功响应为 `*DEV#bound#0#1#`；`bound=0` 分支暂不开放，返回失败响应并保持状态；未认证、格式错误和非法参数请求不改变状态；
- 未调用 `cpu_reset()`、BLE 主动断连、USB/UAC、音频或其他 JL 模块副作用；解绑、恢复出厂、ACK 后延迟断连和 pending action 仍属于第三批。

实际修改文件：

```text
新增  SDK/apps/common/third_party_profile/rdx_protocol/rdx_device_state.c
新增  SDK/apps/common/third_party_profile/rdx_protocol/rdx_device_state.h
新增  SDK/apps/common/third_party_profile/rdx_protocol/rdx_device_management.c
新增  SDK/apps/common/third_party_profile/rdx_protocol/rdx_device_management.h
修改  SDK/apps/common/third_party_profile/rdx_protocol/rdx_identity.c
修改  SDK/apps/common/third_party_profile/rdx_protocol/rdx_ble_transport_br56.c
修改  SDK/apps/common/third_party_profile/rdx_protocol/rdx_mvp0_protocol.c
修改  SDK/apps/common/third_party_profile/rdx_protocol/rdx_protocol_defs.h
修改  SDK/apps/common/third_party_profile/rdx_protocol/rdx_protocol_entry.c
修改  SDK/apps/earphone/include/user_cfg_id.h
修改  SDK/build/genFileList.c
生成  SDK/build/fileList.mk
```

### 18.2 2026-08-08 构建与静态验收

| 检查项 | 结果 |
| --- | --- |
| Windows 干净构建 | `make clean` 通过；`make` 完成编译、链接和固件后处理，末尾因设备离线显示 `Device Offline`，未自动下载 |
| 链接产物 | `SDK/cpu/br56/tools/sdk.elf`、`sdk.map` 和 `sdk.elf.resolution.txt` 已生成；Map 含本批对象，解析表确认 `rdx_device_state_init`、`rdx_device_state_set_bound` 和 `rdx_device_management_bind` 已被引用 |
| 官方库抽取 | 仍仅 `librdxApp.a(rdx_encryption.c.o)`；未出现 `rdx_protocol.c.o`、`rdx_queue.c.o`、`rdx_uxfile.c.o` 或 `xxpUart.c.o` |
| RDX OFF 门控 | 新增源文件和 VM 访问均位于 `TCFG_RDX_ENABLE` 最终门控下；本次完成静态门控核对，未重新执行 RDX OFF 完整 ELF 构建 |
| `git diff --check` | 通过 |
| 固件 | `output/update.ufw`，1,474,048 B |
| 固件 SHA-256 | `8dc666cb5bc6a286e678764ff1a60b53ef15da2a0c840fed012906b00f2ecb9b` |

本次构建生成的 `SDK/cpu/br56/tools/init.bin` 属于下载后处理产物，不纳入功能提交；本批固件后续已在板上完成以下实机验证，板卡、手机/系统和 APP 版本仍需在正式验收记录中补齐。

### 18.3 实机验收状态

测试日志：

- `COM3_2026-08-08_10-42-50.log`：首次启动、首次绑定、重复绑定、USB Speaker 活跃期间绑定及第三批命令误触发核对；
- `COM3_2026-08-08_10-45-44.log`：BLE 断连、Bound=1 广播恢复和重连认证；
- `COM3_2026-08-08_10-53-44.log`：完整冷启动后 VM 读取和 Bound=1 广播恢复。

| 项目 | 结果 | 证据与结论 |
| --- | --- | --- |
| 未绑定冷启动广播 `Bound=0` | 通过 | 10:42 日志读取失败回退默认未绑定，广播记录 `bound=0`，Manufacturer Data Bound 字节为 `00` |
| AppKey 后绑定成功 | 通过 | 收到 `*APP#bound#1#`，VM ID 15 写入返回 4，成功 ACK 为 `*DEV#bound#0#1#` |
| 正常断连后恢复广播 `Bound=1` | 通过 | 10:45 日志记录 `advertising bound=1`，Manufacturer Data Bound 字节为 `40` |
| 冷启动后持久化 `Bound=1` | 通过 | 10:53 日志从 `setup_arch` 开始，记录 `state_load=vm vm_id=15 version=1 bound=1`，随后广播 `bound=1` |
| 重复绑定不重复写 VM | 通过 | 首次写入后重复绑定 4 次均记录 `vm=skip`，均返回成功 ACK |
| VM 写失败时 RAM、广播和失败 ACK 语义 | 未验证 | 当前日志只有成功写入，尚未完成故障注入 |
| USB Speaker 活跃期间绑定不引起断音、重枚举或复位 | 日志层面通过 | 10:42 日志在 `jlstream_0` 运行期间执行绑定，绑定前后未见新增 USB reset、播放关闭或异常复位；主观连续播放和更长压力仍需单独确认 |

10:42 和 10:53 日志均出现启动阶段 USB reset/`setup_interface fail`；这些事件发生在绑定业务前，当前不归因于第二批代码，但仍作为既有 USB 基线问题保留跟踪。

第三批仍未开始，解绑与恢复出厂相关代码和实机项目保持未实施。
